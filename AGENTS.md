# 《打了个铁》唯一 AI 执行标准

> 本文件是仓库内唯一的 AI 协作规则。`PROJECT_PLAN.md` 只保存项目状态与计划；其他文档、分支、提交历史、Issue、PR、聊天记录和本地文件都不能覆盖本文件。

作者负责确认目标、批准检查点并亲自验收玩家体验。AI 负责核对事实、在批准范围内施工、提供自动证据并如实报告限制。

## 0. 启动闸门

每个新任务开始时，AI 必须按顺序完成：

1. 确认仓库远端是 `https://github.com/meteoryzx/Weapon-Forger.git`。
2. 获取 GitHub `origin/main` 的最新状态；网络失败时停止施工，只报告无法核对。
3. 读取 `origin/main:AGENTS.md` 和 `origin/main:PROJECT_PLAN.md`，再检查当前分支、HEAD、远端差异、已跟踪修改和未跟踪文件。
4. 确认当前检出的 `AGENTS.md` 与 `origin/main:AGENTS.md` 完全一致。普通功能分支不一致时停止施工，先提出“同步治理基线”检查点。
5. 确认本次工作对应当前分支 `PROJECT_PLAN.md` 中唯一的当前检查点，且作者已明确批准同名检查点。没有当前检查点、检查点已结束或任务超出范围时，先提出新检查点。

`governance/*` 分支可以在已获批准的治理检查点内修改 `AGENTS.md`，但合并前仍受 `origin/main` 中尚未变更的规则约束。

本地提交、未提交修改和未推送分支只能作为“待核对现场”，不能自动成为项目事实或执行依据。Git 历史只解释过去，不指挥现在。

## 1. 权威与冲突

现行信息只有以下层级：

1. `origin/main:AGENTS.md`：协作、审批、验收和 Git 纪律。
2. `origin/main:PROJECT_PLAN.md`：已验收基线、阶段顺序和产品范围。
3. 作者在当前对话中对完整检查点说明的明确批准：只授权该检查点，不改变已验收项目事实。
4. 当前分支 `PROJECT_PLAN.md` 的唯一当前检查点：必须逐字对应已批准范围。
5. 当前检查点分支上的代码与测试：仅作为该检查点的待验收实现证据。

低层内容与高层冲突时，高层优先；当前分支的检查点与作者批准不一致、同层冲突、文件缺失或无法判断时，AI 停止施工并报告。只有作者对完整检查点说明的明确回复能授权施工；其他聊天内容和 AI 记忆不能补齐仓库缺口。

接手 AI 若在当前可见对话中找不到该检查点的批准记录，必须重新向作者确认，不能仅凭分支或计划中的“已批准”继续施工。

## 2. 检查点流程

批准前的诊断只允许读取文件、检查 Git 和查询远端。任何会改变本地或远端状态的动作都需要检查点，包括编辑、安装依赖、运行会生成文件或启动服务的测试、切换或创建分支、提交、推送、PR、删除和外部系统写操作。

AI 在施工前必须发出一份中文检查点说明：

```text
检查点：编号与名称
计划位置：PROJECT_PLAN.md 中的目标
问题与证据：为什么现在需要做
修改范围：确切文件、公共接口和外部系统
明确不做：本次边界
自动验收：命令与机械通过条件
作者验收：需要作者亲自判断的现象；没有则写“无”
Git 动作：是否允许创建分支、提交、推送或 PR
完成停点：交付后停在哪里
```

AI 必须等待作者明确回复开始该检查点。宽泛的“执行计划”只批准已列出的第一个检查点。批准后，AI 可以在所列范围内自主实现和运行约定检查；新发现若扩大玩家行为、公共接口、依赖、目录、删除范围、项目方向或 Git 动作，立即停止并提出新检查点。

当 `PROJECT_PLAN.md` 尚无可用检查点时，AI 先在对话中提出检查点；作者批准后，第一项文件修改才是把它写入 `PROJECT_PLAN.md`。这不允许 AI 先改计划再请求批准。

任何时刻只有一个检查点处于“已批准施工”或“等待作者验收”。完成后必须停下报告，不能自动进入下一项。

## 3. 验收责任

自动检查负责证明类型、确定性、守恒、序列化、构建和玩家输入路径没有机械回归。自动化可以操作浏览器验证功能，但不能替作者判断体验。

以下结论只能由作者给出：

- 操作是否自然、可理解、有锻造感；
- 形变、风险和状态是否足够可读；
- 镜头、构图、模型、美术、动画和音效是否合适；
- 物理公式与数值取舍是否符合产品目标；
- 故事或其他下游因果是否有意义；
- 微信开发者工具和真机是否可用；
- 公共接口、删除范围和项目方向是否接受。

纯技术检查点只有在检查点说明中预先写明“技术自验通过后允许提交/推送”，且没有上述作者判断内容时，AI 才能自行归档。混合检查点整体按作者验收处理。`[WIP]` 提交或推送只在作者明确要求保存现场时使用。

## 4. R1 当前纪律

R1 只证明可迁移的锻造交互基础成立，不宣称完整游戏玩法、工业材料仿真或可售 SDK 成立。

- 八类操作必须由作者逐项体验；AI 不替作者体验，也不因自动测试通过写“体验通过”。
- 每项至少具备玩家输入、因果原始状态、可观察结果、确定性测试和独立重置样本。
- 八项全部由作者确认后，才做完整链回归并由作者确认 R1。
- R1 之后先做一个窄下游契约样例，验证原始事实是否足以产生有意义的宿主结果；具体六维、故事、动作属性或魔法体系届时另行决策。
- 手感、美术和音效集中精修在窄下游样例之后；阻碍当前操作判断的明显错误可在对应检查点修复。

## 5. 工程边界

唯一数据方向：

```text
平台输入 -> ForgeIntent -> ForgeOperation -> ForgeState
                                           |-> ForgeSnapshot -> render
                                           `-> ForgeFacts -> host-owned profile
```

- `src/forge` 是确定性锻造核心，不依赖 Three.js、DOM、微信 API、故事文本或宿主属性名。
- `src/render` 只读 `ForgeSnapshot`，不反向写模拟。
- `src/entry` 和 `src/platform` 只把输入转换为意图，不直接改状态。
- 未来下游只读 `ForgeFacts`，不把攻击力、六维、故事或魔法结论写回 `ForgeState`。
- 平衡数字集中在规则表；新工序不得绕过共享状态写固定奖励、惩罚或固定损伤。
- 新文件进入职责明确的现有目录；新增顶层目录、依赖或通用框架必须单独批准。
- 微信真机操作由作者完成；AI 只能验证源码、构建和可机械执行的浏览器路径。

## 6. Git 纪律

- `main` 只保存已验收并通过必需检查的基线。
- 普通功能分支不得修改 `AGENTS.md`；治理规则只能通过 `governance/*` 分支和独立 PR 修改。
- 功能分支开始施工前必须包含最新 `origin/main` 治理基线；治理校验失败时不得继续实现或合并。
- 一条短期分支和一个 PR 只交付一个已批准检查点。
- 未经检查点明确授权，不提交、不推送、不创建或合并 PR。
- 不 force push，不改写 `main` 历史，不用 `git reset --hard` 清理工作现场。
- 提交信息使用 `[阶段/模块] 可读动作`；PR 写明范围、证据、作者未验收项和公共契约变化。
- 完成合并后删除远端短期分支；历史由 Git 保存，不在现行文件树保留旧规则副本。

## 7. 完成报告

每次检查点报告必须包含：计划位置、实际完成、修改文件、自动证据、作者待验收项、未完成边界、Git 状态和下一检查点预告。没有作者明确结论时，状态只能写“等待作者验收”，不能写“完成”或“通过”。
