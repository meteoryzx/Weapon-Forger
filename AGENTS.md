# 《打了个铁》开发约定

用户在当前对话中的明确目标、授权和修正优先于本文件。项目以 threejs-game-director 及其生产技能组组织开发；其他技能只在任务需要时调用，不增加重复审批。

## 1. 开发流程

1. 继续工作时读取 PROJECT_PLAN.md 当前范围和 artifacts/game-progress.md 最新现场；检查当前分支、差异和未提交改动，保留他人工作。
2. 先确认玩法结构、功能边界、空间尺寸和参考，再按 director 路由到玩法、图形、界面、调试和 QA 技能。宽范围重构读取完整生产技能组。
3. 在已批准流程内连续完成实现、集成、缺陷修复、适当测试、浏览器截图和真实输入验证。内部技术阶段结束后自动继续，不把阶段汇报当作交付。
4. 只有可运行的作者体验版本准备好，或存在无法自行解决且确实需要用户决定的阻塞时，才结束当前轮。批准和授权在后续“继续”中保持有效。
5. 更新只说明已经做了什么、用户现在需要做什么、下一步做什么。结束回复包含可用入口、待体验现象、已验证事实与剩余限制。

## 2. 当前工程原则

- PROJECT_PLAN.md 保存产品范围、当前阶段与作者验收状态；artifacts/game-progress.md 保存施工现场和下一动作。旧测试结果不能证明新代码有效。
- 以游戏效果和正确流程为目标。已授权重构时，旧模型、目录习惯、坐标和局部“已通过”结果不能阻止替换错误实现。
- 模型采用共享毫米尺寸契约，统一转换为场景单位。房间只有一份，每个工位只有一个实体；工件保持真实尺寸，近景通过相机切换。
- 空间验证测量实际模型边界、工作面接触、墙体间距和交互对象；不能只比较声明的占地框，或用改摄像机、隐藏邻居掩盖穿模。
- 视觉验收使用当前代码的桌面和窄屏截图、运动证据和渲染统计。测试断言依据物理或玩法契约，不为追求通过而迁就错误坐标。
- 八类锻造操作保留产品结构。焊合暂缓为选料桌上的未来高级工艺；动力锤归属锤击，当前只有外观预留，不自行增加玩法。
- 用户提供的参考图约束风格和设备结构；已确认的尺寸契约优先于生成图片里的尺寸标注。

## 3. 核心边界

平台输入 -> ForgeIntent -> ForgeOperation -> ForgeState -> ForgeSnapshot / ForgeFacts。

- src/forge 保持确定性，不依赖 Three.js、DOM、微信 API 或宿主玩法属性。
- src/render 只读快照；src/entry、src/platform 将玩家输入转成意图。
- 保留材料身份、体积守恒、温度与形变事实、存档兼容和已有操作语义。涉及这些契约的修改需要对应回归证据。
- 自动检查负责类型、几何、确定性、构建与真实输入路径；作者负责比例、美术、操作感受和微信真机判断。

## 4. Git 与协作

- 未提交现场是可继续开发的真实工作，main 保存已合并基线。远端规则不能覆盖用户当前明确指示。
- 仅在发布、提交、推送或合并涉及已授权范围时执行相应 Git 动作；准备可审阅结果后再处理尚未授权的外部动作。
- 保留无关修改；不强制推送，不改写 main 历史，不用破坏性重置清理现场。
- 仓库只维护这一份项目级 AI 约定。check:governance 检查文档结构，不用远端文件逐字一致性阻塞已授权开发。
- 需要模型切换时遵循用户偏好；普通继续不重复推荐，也不因此暂停。
