# 《打了个铁》AI 工作规范

本文件给编码 AI 阅读。作者不需要写代码，作者的职责是：说明目标、确认边界、亲手验收玩家行为。AI 的职责是：在边界内施工、提供可验证证据、如实报告限制。

每次开始任何工作前，AI 必须完整阅读本文件和 `PROJECT_PLAN.md`，再检查 Git 工作区状态。文档权威顺序固定为：`AGENTS.md` 管协作纪律，`PROJECT_PLAN.md` 管阶段与状态，`docs/ARCHITECTURE.md` 管模块边界，`docs/FORGE_CORE_API.md` 管公共契约，`docs/FORGE_SYSTEM_SPEC.md` 管八类能力的产品语义。代码、测试和 Git 历史用于核验实现事实；聊天记录和 AI 的短期记忆不能作为交接依据。发现冲突时必须报告并停止依赖冲突部分，不得自行挑选一种解释。

## 1. 开发范式：八项验收、整链回归、下游探针、集中精修

R1 只证明“源码层可迁移的锻造交互基础成立”，不宣称完整玩法、工业材料仿真或可售 SDK 已成立。当前阶段把八类锻造能力分离为固定样本，由作者逐项体验和验收；八项通过后做整链回归并归档 R1，再用一个窄下游 profile 验证 `ForgeFacts` 的消费价值，最后集中优化手感、美术和音效。

必须遵守：

- **一切可验证**：每次交付必须给作者可操作的证据，例如可点击入口、固定操作步骤、截图、录屏或可读状态输出；不能只说“写完了”。
- **接口先行**：跨模块数据如何传递，先写进 `PROJECT_PLAN.md`；模块内部可以迭代，但不得偷偷改变对外契约。
- **配置与逻辑分离**：材料、工具、规则阈值、故事节点和文案属于数据；计算和读取数据的程序属于逻辑。不得把策划数值散写在代码中。
- **先验证核心，再填内容**：先跑通八项锻造能力和完整链，再用一个窄下游样例验证消费价值；具体属性体系、故事、美术库、内容量和分享功能后置。

### MVP 先行与物理基线保留

- 当前施工顺序固定为：八项操作逐项验收 → 完整链回归 → R1 归档 → 一个窄下游契约探针 → 手感、动画、音效和美术集中精修。
- MVP 可以简化场景、工具模型和反馈，但必须继续使用 `ForgeIntent`、`ForgeOperation`、`ForgeState`、`ForgeSnapshot` 和 `ForgeFacts` 这些公共契约。
- 当前 `physics-2` 统一材料响应层和 XPBD 点阵几何层是长期底层资产，不是一次性原型。MVP 不得删除、绕过或为单个工序另写孤立的加减规则；后续打磨优先校准参数、载荷和呈现。
- 除非固定样本证明公共契约或物理模型存在结构性错误，否则不得因为单次试玩反馈就进行整套模型重构；问题先记录到 MVP 缺口，在八动词闭环后集中处理。
- 每个新增动词必须同时交付玩家输入、原始状态变化、可见结果和自动测试；只改文字、计数或评分而没有操作因果的实现不算完成。
- “可操作”不等于“点击工位立即提交意图”：每个工位必须有独立构图、相机切换、当前工件与工具的可见关系，以及至少一种连续的点击、按住、拖动或调整过程；自动测试不得只通过直接调用或坐标点击方块来冒充玩家操作。
- 正式下游属性、故事、正式美术和音效不提前进入本 MVP；下游只能消费已稳定的原始状态。

## 2. 工作模式：诊断、批准、施工、验收

作者反馈首先授权 AI 做只读诊断和测试。AI 必须先报告问题性质、修改范围、验收方法和边界，获得作者明确批准后才能编辑；批准后可在该范围内自主完成实现和验证。八项操作验收每次只处理一个切片，作者确认该项通过后记录状态，再进入下一项；八项全部通过前不得宣称 R1 基础操作验收通过。

### 2.1 批准边界

- 一次批准只覆盖报告中列明的目标、文件边界和测试范围；同一修复中的必要机械迁移无需逐行确认。
- 新发现若会扩大工序、公共接口、依赖、目录、删除范围或玩家行为，必须另行报告并等待批准。
- 范围之外的任何新增（新工序、新架构层、新依赖、新目录、删除既有功能、变更公共接口、仓库可见性、许可证）仍需单独说明并获作者同意，不因 Demo 授权而自动放大。

### 2.2 自主施工期间的防幻觉机制（替代逐片确认）

AI 在施工中必须做到：

- **确定性优先**：`forge` 和未来宿主规则保持可重复计算的纯 TypeScript；同一操作回放两端得到相同 JSON。
- **每个纯逻辑模块配自动测试**：新规则必须能用单测断言「同一输入 → 同一输出」「合法输入不报错」「非法输入被拒绝」。测试通过是继续施工的前提，不是事后补丁。
- **如实报告能力边界**：AI 不得声称完成自己无法验证的事——微信开发者工具/真机、手感、数值取舍、美术风格、故事因果的「好不好」判断，只能由作者在最终验收给出。
- **遇到两次无法解释的失败即停**：同一路径连续两次无法解释的失败，停止试错，用通俗中文报告哪里失败、已知证据、可能原因、备选方案、需要作者决定什么。
- **不做不可逆动作**：删除文件、关闭 PR、删除远端分支、修改公共接口、force push、改写 `main` 历史，必须先列出精确目标并获作者同意；优先用新分支、`git revert` 或保留现场的 `[WIP]` 提交。

### 2.3 八项操作验收

- Demo 完成、全自动检查通过后，AI 提供左侧验收中控台和八个固定样本。每项交付都必须包含当前完成度、准备状态、操作方法、应看到、不应看到、自动证据和作者需要判断的体验项。
- 验收不通过时，先诊断并报告，只提出当前操作或被固定样本证明存在结构错误的共享底层修复；作者批准后实施。
- 单项通过后更新 `PROJECT_PLAN.md` 的验收台账并保留回归测试；已经通过的项目不得被后续修改悄悄破坏。
- 八项全部通过后再执行完整链回归和 `npm run check`，由作者确认“R1 基础操作验收通过”，随后才归档、推送或进入下一阶段。

## 3. 模块职责与数据流铁律

当前只保留实际闭环必需的六个代码边界：

| 边界 | 负责什么 | 明确不做什么 |
| --- | --- | --- |
| `platform` | 画布、时间、输入、存储、音频、分享等平台差异 | 不直接改锻造状态 |
| `app` | 组装依赖、控制游戏流程 | 不存物理规则或渲染细节 |
| `forge` | 锻造意图、合法操作、确定性状态、回放 | 不算六维、不讲故事、不依赖 Three.js |
| 下游 profile（后续切片） | 把 `ForgeFacts` 翻译为某个宿主需要的属性 | 不反向修改锻造事实；六维只是候选方案 |
| `story`（待设计） | 未来消费某个宿主的派生数据 | 当前不规定结构，不读取点击次数编造事实 |
| `render` | Three.js 程序化场景与状态可视化 | 不反向写任何游戏状态 |

唯一允许的数据方向：

```text
平台输入 -> ForgeIntent -> ForgeOperation -> ForgeState
                                           |-> ForgeSnapshot -> render
                                           `-> ForgeFacts -> downstream profile
```

铁律：

- `forge` 与后续下游规则必须是可重复计算的纯 TypeScript，不得导入 Three.js、浏览器 DOM 或微信 API。
- 平台层只把鼠标、键盘、触摸转换成 `ForgeIntent`（锻造意图），绝不直接改状态。
- `render` 只读取 `ForgeSnapshot`，不得写入模拟或下游属性。
- 未来故事只能读取宿主明确提供的派生数据和种子，不能根据“玩家乱点了几次”编造后果。
- 所有公共状态必须可序列化、带规则版本；同一操作回放在浏览器与微信端必须得到相同 JSON。
- 不新增 Manager、Service Locator、Factory、事件总线、编辑器或公共框架层，除非出现第二个真实需求并获作者同意。

### 仓库结构与所有权

目录结构是长期契约，不随单个切片临时扩张：

| 路径 | 唯一责任 |
| --- | --- |
| `src/app` | 组装模块并控制游戏流程 |
| `src/entry` | 浏览器与微信运行入口 |
| `src/platform` | 平台输入、时间、存储、音频与分享适配 |
| `src/forge` | 锻造意图、操作、状态、规则与回放 |
| 下游模块（尚未命名） | 未来由真实宿主把 `ForgeFacts` 翻译为所需属性；当前不预设目录 |
| `src/story`（候选） | 只有产品决定采用故事消费方式后才创建 |
| `src/render` | Three.js 场景与只读状态可视化 |
| `src/data` | 宿主游戏的内容与文案数据；锻造核心自有材料和物理规则留在 `src/forge` |
| `tests` | 按 `src` 边界镜像组织的测试；端到端测试只放 `tests/e2e` |
| `scripts` | 无运行时依赖的开发、验证和数据脚本 |
| `wechat` | 微信小游戏宿主配置，不放业务逻辑 |
| `.github` | CI、PR 和 Issue 协作配置 |

- 根目录只放项目入口文档、包管理与构建/测试配置，不散放业务源码或临时记录。
- 新文件必须归入拥有该责任的既有模块。不要创建 `utils`、`common`、`managers`、`services` 等含糊的兜底目录。
- 公共类型和模块名描述稳定业务含义，不包含 `S3a`、`R1i` 等阶段编号；阶段只属于计划、Issue、PR 和提交信息。
- 新增顶层目录、改变依赖方向或移动模块所有权，必须先作为结构变更说明迁移范围并获得作者同意，同时更新本节和 `README.md`。
- 删除功能时一并删除失效测试、配置和文档引用；Git 历史负责存档，不在现行文件树保留“备用旧版”。

## 4. 已锁定技术与内容边界

- 正式目标是**微信小游戏**；浏览器 H5 只是同源代码的调试、自动验收和作品集入口，不是以后再迁移的临时原型。
- 技术栈是 TypeScript、Three.js、Vite、Vitest、Playwright。首轮场景、钢坯、器具、特效和生产 UI 均由代码和参数生成。
- 不使用 Cocos、Unity、外部场景编辑器、GLB 模型、贴图资产或生产环境依赖的 DOM 界面。
- 微信适配只实现本项目需要的画布、WebGL 上下文、尺寸、帧循环、触摸、离屏画布、本地存储，以及音频/分享接口预留；不模拟完整 DOM、不改 Three.js 源码、不用依赖 DOM 的 Loader 或控件。
- 不做有限元、软体物理、真实化学、任意拓扑、开孔、分叉、折叠焊接、运行时 LLM、经济成长、支付、广告、服务端或大量正式美术。
- 动力锤、自由焊接和受控装配是后续设计项，不得抢在最小锻造链之前实现。

## 5. 工程质量、验证与止损

- Demo 范围内每个纯逻辑模块都要有自动测试；作者能按中文步骤完成八项单独验收，并在全部通过后完成一次整体回归。
- 每个模块要有独立测试入口；不能要求作者先跑完整游戏才能验证一个小功能。
- 微信端是否可用，只能由作者在微信开发者工具和真机预览确认。AI 不得声称完成了自己无法进行的 GUI 或真机验证。
- 依赖和版本必须锁定；生成目录不得进入 Git；源文件、配置和必要元数据必须进入 Git。

## 6. Git 与跨设备交付

- Demo 施工集中在一个短期分支上，最终以一个 PR 交付「一条完整可玩闭环」。
- 提交信息使用 `[阶段/模块] 可读动作`，例如 `[R1/forge] 修正焊合工件选择`。
- `npm run check` 全绿、作者体验验收通过、完整差异检查通过后，PR 才 squash merge 到 `main`。
- AI 完成改动后先报告文件、测试和未验证边界；只有作者明确同意归档时才提交、推送或创建/更新 PR。
- `main` 必须始终保持可运行；Git 历史就是档案，不创建额外的流程文档堆积。
- 换电脑前必须关闭编辑器、运行已知检查、更新 `PROJECT_PLAN.md`、推送已验收提交或一个明确的 `[WIP]`，并核对本地与远端提交一致。

## 7. 沟通、模型与能力边界

- 所有说明、错误和取舍都用通俗中文；术语第一次出现时说明其用途。
- AI 必须如实说明能力边界。账号登录、微信开发者工具导入、真机扫码、真机试玩由作者操作；AI 只能提供步骤与检查点，不能虚构 GUI 控制能力。
- 架构、物理规则和复杂故障分析适合高推理模型；进入重复编码、常规测试、格式整理时，AI 应提醒作者切换低消耗模型。
- 作者提出的新想法先记录为“待讨论”，不得在 Demo 施工中顺手加入；先完成当前最小闭环再扩展。
