# 《打了个铁》项目计划与交接

> 本文件保存当前有效的产品设计、工程边界、阶段状态和跨设备交接信息。它替代旧的分散交接文档；旧 Git 历史仍保留，但不再作为当前执行依据。

## 当前执行补充：可迁移锻造核心

当前产品优先级已明确调整为：先完成八个锻造动词的状态、回放和最小可读表现，再建立从原始状态到下游属性的可扩展派生接口，最后集中优化手感、美术和音效。

`src/forge/index.ts` 是可迁移锻造核心的公共入口。核心不得依赖 `app`、`render`、`entry`、`platform`、Three.js、浏览器、微信、故事文本或某个游戏的专有属性。动作游戏的攻击力/破甲/防御，冒险游戏的六维和故事条件，或魔法系统的奥术亲和/符文承载，都必须通过下游 profile 消费 `ForgeFacts`，不能反向污染 `ForgeState`。

这项边界优先于本文件中早期关于“R2/R3 已实现”或“下一步直接制作六维/故事”的旧描述；当前仓库只交付原始状态和可扩展派生接口，六维与故事仍是下游切片。

## 当前架构决策（2026-08-21）

本项目下一阶段不是继续增加独立按钮或各工序的局部加减规则，而是验证一套统一的锻造过程模型：八个工艺类别共享几何、质量、温度、应力、塑性、材料组成、损伤、表面和界面状态；不同工艺只提供不同的工具接触、运动和热边界。

统一模型的研究基线见 `docs/FORGE_UNIFIED_PHYSICS.md`，代码分层、文件所有权、公共契约和交接规则见 `docs/ARCHITECTURE.md`，核心 API 见 `docs/FORGE_CORE_API.md`。这三份文档是设计讨论和后续实现的入口，不代表其中所有公式已经实现。

### 当前优先级

1. 校正仓库事实和交接入口，避免把 WIP 写成已验收功能。
2. 以第一人称锤击工作站验证统一过程模型：落点、支撑、旋转、送料、温度和材料流动必须产生可见且可回放的结果。
3. 在同一核心上补齐切割、焊合、加热、淬火、回火和研磨的独立操作，不为每个工艺创建孤立的质量分数。
4. 通过固定样本验证质量/体积、热量方向、局部变形、低温风险、界面完整性和表面去除。
5. 统一模型和第一人称操作获得作者体验验收后，再进入完整 R1 工艺链和体验精修。

### 当前明确不宣称

- 当前分支不是已验收 R1 完整链；
- 当前画面是已通过自动检查的第一人称热锻工作站原型，尚未完成作者体验验收和最终表现打磨；
- 当前点阵/XPBD 不是完整材料仿真；
- 当前损伤、焊合、切割和研磨仍需按统一模型继续校正；
- `npm run check` 全绿只能证明自动契约稳定，不能代替作者对真实感、可读性和手感的验收。

## 0. 当前模式：Demo 先行（一次闭环，一次验收，再精修）

- **工作模式**：逐片确认制已停用。总计划经作者确认后，AI 自主、连续完成 Demo 范围施工，以「自动测试 + 确定性契约」防幻觉，最后做一次 Demo 级体验验收，再进入精修。细则见 `AGENTS.md` 第 2 节。
- **正式基线**：`main` 的 `2cb279f` 已归档截至 `R1h-3a` 的稳定锻打与仓库结构基线；火炉热学 WIP 在 `feat/R1i-furnace-heating`。
- **接手状态**：Windows 已安装 Node 24、依赖和 Playwright Chromium。`main` 通过 21 个逻辑测试、双端构建和 4 个端到端测试；火炉 WIP 通过 25 个逻辑测试和 3 个火炉端到端测试。注意：火炉分支存在一条本地未推送提交，开工时先处理。

### Demo 目标与范围（本次一次做完）

**目标**：玩家在浏览器中走完 R1 最小可玩锻造链——选料 → 加热 → 锻打 → 淬火 → 研磨 → 产出一份原始状态（外观形变后的武器 + 内部原始数值）。六维（R2）、故事（R3）不在本 Demo 范围。

**本次做**：

| 工序 | 内容 | 状态 |
| --- | --- | --- |
| 选料 | 2 种材料（低碳钢/高碳钢），可硬化潜力、韧性、重量有可感差异 | 新增 |
| 加热 | 沿用火炉（整块均温、送入/取出/热色） | 已有，并入 |
| 锻打 | 沿用锤击形变（转面/送料/蓄力/局部形变） | 已有，并入 |
| 淬火 | 水/油两介质；温度骤降、硬化与脆性风险；时机影响结果 | 新增 |
| 研磨 | 沿刃口滑动；覆盖率→锋利/刃口连续性；不均→缺陷 | 新增 |
| 回火 | 回火温度 → 硬↔韧（状态记录，六维解释在 R2） | 新增 |
| 切割/焊合 | 切割一分为二、焊合按体积混合 carbon + 层数（大马士革涌现点） | 新增 |
| 编排 | 最小线性流程把各工序串起来（浏览器先行） | 新增 |

**后置（R2/R3，不在本次）**：六维评估（`evaluate`）、故事（`story`）——本次只交付「原始状态」，不实现六维与故事。

**本次不做**：微信真机验收（只保证 `build:wechat` 通过 + 源码可导入，真机仍由作者做）、工坊总览中枢、局部分段温差、氧化皮画面、烧损、火花/音效精修、存档/分享、正式美术、回火/正火/焊接/装配、服务端防篡改。

**一次验收（Demo 完成时）**：

- 机械项（AI 自证）：`npm run check` 全绿——typecheck + 单测 + e2e + 浏览器/微信构建。
- 体验项（作者一次判断）：能连续锻出一把武器不出错；至少两种操作路线产生肉眼可见且数据可解释的差异（体现在原始状态，如碳、层数、裂纹、刃口）；生产过程不显示六维数字或「优秀/失败」总评。

**完成后停点**：AI 停止施工，输出阶段对齐报告 + 作者测试清单，等作者一次「验收通过」；通过后归档，进入精修改进。

## 1. 项目定位与交付边界

**一句话定位**：玩家是一名铁匠，自由组合锻造工艺打造武器；武器由可解释的隐藏六维性能决定一段冒险故事，并可成为分享内容。

这是系统策划秋招作品集。第一优先级始终是“设计深度 + 一条完整、可玩的核心闭环”，不是商业化完整度。

锻造规则核心同时是一项可长期积累的系统资产：在不牺牲当前游戏、不提前建设通用框架的前提下，`forge`、`evaluate` 与规则数据应保持可迁移；当《打了个铁》已经证明其玩法价值并出现第二个真实使用场景后，再进入独立打包、授权或合作开发。可迁移性是从现在遵守的边界，产品化是 MVP 完成后的阶段，不能倒过来挤占 R1 至 R4。

两大卖点：

1. 自由、可重复、能看见因果的锻造过程：玩家敲在哪里、何时加热、如何淬火、如何研磨，钢坯就以对应方式变化。
2. 数值驱动、可解释的冒险故事：故事不是随机夸奖，而是武器状态的唯一明确反馈。

本轮最终产物必须能在**浏览器 H5**和**微信小游戏**运行；它们共享游戏逻辑、Three.js 场景、程序化模型、视觉参数和数据。浏览器是调试、自动验收与作品集入口，不是未来迁移前的临时原型。无需正式上线微信、支付、广告或服务端。

## 2. 玩家真实游玩流程

最终体验是固定斜俯视的 2.5D 铁匠铺。画面中没有玩家角色；玩家看见工作台、材料架、熔炉、铁砧、淬火槽和磨石等器具。

```text
进入工坊
-> 点击材料架：选择材料
-> 点击熔炉：加热并观察热色
-> 点击铁砧：钢坯置于铁砧面上，玩家令其转面并用锤子落锤塑形
-> 点击淬火槽：拖动钢坯完成淬火
-> 点击磨石：沿刃口研磨
-> 得到一把自由形状、带隐藏性能和事实缺陷的武器
-> 雇 NPC 冒险，观看武器造成的故事后果
-> 后期可分享给好友观看同一武器的故事
```

R1 不先制作完整工坊总览，而是用程序化近景依次跑通选料、加热、铁砧、淬火、研磨；R4 再把这些近景接回可点击的 2.5D 工坊中枢。

锻打近景的当前核心构图：铁砧居中、钢坯斜放于砧面、锤子在右上方；已确认移除钳子，钢坯须原地围绕自身长轴转面。PC 当前可鼠标短按轻击、长按蓄力、`A/D` 每次转面 90 度、`W/S` 沿钢坯长轴送料；浅色锤面预览由鼠标悬停位置决定。微信当前有明确转面控件，拖动送料后续实现。两端输入都只能生成相同的 `ForgeIntent`，不能直接写游戏状态。

## 3. 设计原则：自由，但不黑箱

锻造是“操作为地基、工艺为配方”；故事是“武器数据为接口、内容为配方”。系统要让玩家组合出多样结果，而不是让玩家寻找唯一标准答案。

- **工作台状态机，不是固定流水线**：材料对象持有当前状态；工具只在状态合法时亮起。以后新增工艺，只增加操作和门槛，不推翻地基。
- **透明的自由**：自由不是瞎试。温度色、局部形变、裂纹、火花、蒸汽、研磨反光、声音等都要让玩家理解发生了什么；失败必须可读。
- **评判执行，不评判意图**：操作精度有客观后果，例如低温重锤更易积累损伤、淬火时机错误会影响热处理、研磨不均会留下刃口问题。玩家想锻成匕首、弯刀或奇怪的“像斧子的剑”都合法，系统不以预设造型评判其价值。
- **故事用后果说话**：玩家过程界面不显示“优秀/失败”总评和六维数字；最终由 NPC 冒险中的卷刃、断裂、失衡、威慑或劈砍后果揭示武器。
- **每个工序形成决策闭环**：检查点必须说明玩家当下的目标、可用动作、阻力与取舍、可见信息、实际状态变化，以及它会怎样影响后续工序或故事。功能能运行但不能改变玩家判断，不算玩法验证完成。
- **手感分层投入**：操作确认和状态可读属于当前规则的必要接口；风险提示在 R1 整链中验证；只增强爽感的震屏、音效、镜头与正式美术在核心闭环成立后集中打磨。

首版最小工艺链：

```text
选料 -> 加热 -> 锻打 -> 淬火 -> 研磨
     -> 隐藏六维与事实缺陷 -> NPC 规则冒险 -> 故事后果
```

回火、正火、受控装配、动力锤、图纸、折叠焊接和自由焊接都在最小链稳定后讨论；它们不得提前挤占核心验证时间。

## 4. 游戏物理、形变与六维评估

### 4.0 真实物理与游戏物理的平衡点（Demo 执行准则）

**平衡原则：真实在因果结构，游戏在精度与可读性。**

- 保留真实锻造的「因果骨架」：敲哪里 → 哪里形变；何时淬火 → 硬度与脆性；怎么磨 → 刃口。这些决定玩家选择是否有意义，不得退化成「按按钮 +1 锋利」。
- 数值与阈值是游戏旋钮，不是物理常数：保留定律的「形状」（辐射 T⁴、Arrhenius 氧化、温变比热）以产生正确的曲线形态（边际递减、过热失控），但系数、阈值、时间倍率按「玩家能在一局内看见并学会差异」调。
- 每条物理后果必须有可读信号：不出现玩家看不见的裂纹、无预兆的失败。

各工序落点：

| 工序 | 保留的真实因果 | 游戏化的部分 | Demo 决定 |
| --- | --- | --- | --- |
| 加热 | 对流+辐射热学形式、温变比热 | 时间倍率压缩、温度是游戏可读的热色 | 整块均温（标量温度）；局部温差后置为第一精修项，接口预留 |
| 锻打 | 空间因果、体积守恒、热塑冷裂 | 应力/损伤/裂纹阈值是游戏旋钮 | 保留现有 XPBD 求解器，不加深 |
| 淬火 | 奥氏体化温度窗口→硬化、水比油剧烈、过热→脆裂 | 硬度/脆性用 0–1 游戏值表达 | 不做马氏体曲线 |
| 研磨 | 磨薄刃口、形成连续刃、不均→缺陷 | 锋利/均匀度用 0–1 游戏值表达 | 表面状态（覆盖率），不改节点几何 |
| 六维 | 从真实状态事实推导，每维可追溯来源 | 维度是游戏抽象，不设总分 | 见 4.3 |

**可调性承诺**：所有平衡相关数字集中在 `src/data` 与 `FORGE_RULES`；作者在体验时给的建议 = 改数据表，不是重写代码。

### 4.1 采用的“够用且可扩展”的模拟

不做有限元、软体物理或真实化学。我们模拟对玩家操作和故事后果真正有用的四类状态：

| 状态 | 保存什么 | 玩家能看见什么 |
| --- | --- | --- |
| 材料 | 密度、可硬化潜力、热参数、基础韧性等 | 材料外观、加热与成形手感差异 |
| 几何 | 截面宽度、厚度、长度分布、中心偏移、质量分布 | 刀尖、刀腹、刀背、刃口、弯曲、柄茎与重心 |
| 热处理 | 温度、塑性、淬火历史、后续回火预留 | 热色、冷却、蒸汽、硬化结果 |
| 完整性与表面 | 应力、损伤、裂纹、研磨连续性、表面完成度 | 裂纹、卷刃风险、磨痕、金属光泽 |

钢坯使用真实尺寸的三维规则网格，当前动力锤后薄坯为 `336 × 48 × 8`，单元边长为 2 个世界单位，共 `168 × 24 × 4` 个六面体。相邻单元在长、宽、厚三个方向共享节点；每块记录局部体积、温度、塑性、应力和损伤，几何尺寸与中心位置从共享节点推导。渲染读取同一网格的边界节点形成封闭实体。首版支持刀尖、刀腹、刀背、刃口、弯曲和简单柄茎；不支持开孔、分叉和任意拓扑。

每次锤击只求解锤面覆盖区与外围缓冲单元：带圆滑边缘的冠形锤面先接触当前高点，再随锤面下降扩大接触集合；有限砧面只为实际落在其投影内的材料提供反向支撑；单元有向体积约束令受压材料向自由方向流动；接触摩擦限制贴近锤面和砧面的切向滑移。热且可塑时锤面能推进得更深，低温重击更容易积累应力与损伤。实际几何体积必须在数值容差内守恒，所有操作固定迭代并可确定性回放。

整块均温的首版加热采用热量守恒：`m * Cp(T) * dT/dt = Q_convection + Q_radiation`。质量来自材料密度与实际体积，表面积来自当前共享节点外表面；炉内读取炉气和炉壁温度，炉外读取环境温度。辐射项使用开尔文四次方，`Cp(T)` 使用材料的分段 Shomate 系数。物理时间固定步长积分，游戏只用独立倍率压缩等待时间。氧化剂量使用温度相关的 Arrhenius 时间积分，当前只记录事实，不渲染氧化皮。

这不是“认真热锻/乱敲”这样的主观按钮扣分。测试中可以用“正确操作序列、低温重击、单侧重击”等描述场景；游戏里结果只来自玩家实际输入和当前材料状态。

### 4.2 参数化伪形变与画面反馈

玩家需要相信“我砸这里，这里真的变了”，但不需要真实材料仿真。技术上用截面参数驱动 Three.js 动态网格：物理截面是事实，视觉截面只是平滑插值。

每次落锤必须在 100ms 内出现局部形变、热色变化、火花与锤击声；淬火出现蒸汽；裂纹有清晰的视觉与声音提示。连续 30 次锤击不得出现断层、空白或报错。若动态网格在两天内无法稳定，画面可降级为连接式 2D ribbon，但不得改变 `ForgeState` 与 `ForgeSnapshot` 接口。

研磨沿刃口滑动，先以程序化表面状态表现“暗哑覆盖被逐步磨开”：覆盖率影响刃口完成度，均匀度影响外观与局部缺陷。不是点击一下就直接加锋利。

### 4.3 隐藏六维是“翻译层”，不是根基属性

最终状态由 `evaluate` 模块推导为 `WeaponData`：

| 六维 | 主要来源 | 故事中的可见后果 |
| --- | --- | --- |
| 锋利 | 刃口几何、研磨连续性、硬度 | 切割、砍断、卷刃 |
| 硬度 | 材料与淬火状态 | 抗变形、硬碰硬、破甲 |
| 韧性 | 材料、热状态、损伤与裂纹 | 格挡、久战、恶劣环境、断裂 |
| 重量 | 密度与实际体积 | 跋涉、敏捷、偷袭、桥梁负担 |
| 平衡 | 质量分布与重心 | 连续动作、疲劳、失衡 |
| 外观 | 几何协调、表面完成度、工艺痕迹 | 威慑、社交、身份误判 |

六维对玩家隐藏，浏览器 `?debug=1` 仅供作者检查公式输入、结果和缺陷来源。`traits` 与 `flaws` 必须结构化保存，例如薄刃、前端过重、弯曲、裂纹、连接不良（后续装配阶段）。不设置“总战力”；若未来保留总评分，也只能用于展示门面，不能替代故事中的逐维判定。

物理取舍必须存在，不能把六维轻易全满：狠淬可提高硬度却增加脆性风险；更薄的刃口更利却更容易崩卷；减料可变轻却降低抗造能力。平衡和外观是“工艺把资源分配得是否协调”的结果，不是任意形状的惩罚。

特殊或魔法功能（如淬毒、引火、认主）不塞进物理六维，应通过工艺标签或后续故事规则单独表达。

## 5. 故事、成长与分享

### 5.1 最小故事闭环

R3 先做一个 NPC、三类情境、3 至 5 个确定性事件。故事只读取 `WeaponData` 和随机种子：同一武器与种子必须得到相同事件；故事不得读取玩家点击过程，也不得编造数据中不存在的缺陷。

故事的乐趣来自反差、因果、代入和稀有结果。例如外观出众但锋利差的武器可以威慑强盗，却砍不断障碍；自己造成的裂纹会在冒险格挡时断裂。冒险“难”主要是结局稀有度，而不是让大部分武器无法走完流程。

完整方向是轻跑团：`WeaponData + 世界状态 + 玩家选择 -> 条件规则 -> 事件 -> 结局`。不做剧情指数级大分叉；故事内容通过情境库、六维分段、条件文案和事件模板扩展。

### 5.2 成长与社交（后置）

后续材料与工具提升的是工艺上限和损耗控制，不应该硬锁基本操作。图纸是可忽略的新手提示，不是唯一合法配方。失败可以产出裂纹和滑稽故事，是内容而不是纯惩罚。

最终有两个出口：玩家雇 NPC 自己看故事，获得即时反馈；或分享给好友查看已确定但对好友未知的故事。R4 才处理微信分享数据、浏览器 URL/二维码与存档；本轮不做服务端防篡改。

## 6. 画面与美术方向

2.5D 是降低资产成本并保留真实感的方式，不是把游戏退回纯 2D。近景锻打强调金属形变、温度、火焰和工具；工坊总览强调器具可读性；冒险画面后期可用剪影分层强化武器焦点。

视觉借鉴《小丑牌》的优点：信息层次清楚、反馈鲜活、有独立气质、像手工打磨过；**不复制**其 2D 卡牌美术。目标是略带像素颗粒/后处理、清晰描边、金属高光、暖色炉火主光源与统一的锻造奇幻气质。避免纯低保真像素和纯扁平矢量。

首轮完全程序化生成，不使用外部模型或贴图。R4 再通过代码生成的描边、光照、色板、火花、蒸汽和程序音效建立统一风格。未来如引入正式美术，先做一个“风格样张”作为所有资产的参照；风格统一优先于单张精美。美术若无法稳定达到精致像素感，应退到“简洁形状 + 厚描边 + 暖色光影”，不为模仿某款游戏硬耗时间。

美术介入节奏：R1a 只保留可读的基础金属与热色，服务于操作验证；R1b 制作并由作者验收一张可玩的锻打近景构图样张，当前确认“铁砧居中、无钳子、钢坯原地转面、右上锤子”的空间关系，器具精细建模与统一风格仍后置；R4 才把已通过的方向扩展为完整工坊和故事呈现。平台头像不属于游戏内美术资产，不影响这条节奏。

## 7. 架构契约与数据归属

```text
BrowserRuntime / WechatRuntime -> GameApplication
平台输入 -> ForgeIntent -> ForgeOperation -> ForgeState
                                      |-> ForgeSnapshot -> render
                                      `-> evaluate -> WeaponData -> story
```

公共契约：

- `ForgeIntent`：平台无关的送料、转面、落锤、蓄力、拖动淬火、研磨等意图。
- `ForgeOperation`：合法性判断后的确定性操作记录，可保存和回放。
- `ForgeState`：材料、24 截面、热处理、完整性、表面、阶段和规则版本。
- `ForgeSnapshot`：仅供渲染读取的几何、热色、裂纹与表面数据。
- `WeaponData`：形状摘要、隐藏六维、`traits`、`flaws`、材料和规则版本；没有总战力。
- `StoryResult`：种子、实际事件、武器因果和结局。

材料参数、物理参数、六维映射、故事节点与文案必须进入带运行时校验的数据文件。纯逻辑模块禁止导入 Three.js、浏览器对象或微信 API。渲染只能读取快照，不能反向改数值。

### 7.1 可迁移边界

可迁移的目标不是把整个游戏做成通用引擎，而是让已经验证的锻造核心能被另一个宿主调用：

- `forge`、`evaluate` 及其规则数据不得依赖 `app`、`story`、`render`、浏览器、微信或《打了个铁》专用文案。
- 宿主通过明确的意图、操作、状态、快照和评估结果使用核心；公共契约可序列化、带规则版本，并有固定样本和确定性测试。
- 平台输入、Three.js 表现、故事内容和产品流程属于适配或应用层，不反向进入核心规则。
- 在第二个真实使用场景出现前，不新增插件系统、通用编辑器、动态扩展框架或独立 npm 包；需要复用时先写出具体接入需求，再决定最小抽取范围。
- R1 至 R4 内部重构只服务当前可玩切片；正式的独立打包、文档和授权交付属于 R5。

## 8. 阶段计划与作者验收

凡检查点涉及新玩家行为或体验验收，AI 的交付回复必须先给作者一份“阶段对齐报告”，不能只汇报写了哪些文件：

```text
当前阶段与检查点：在总计划中的位置
原计划目标：本切片原本要证明什么
本次实际完成：玩家现在确实能做什么
已有证据：自动检查、可观察结果与作者已确认项
尚未验证：不能宣称完成的内容
偏离判断：无偏离，或范围 / 架构 / 阶段 / 证据 / 精度偏离
当前体验精度：操作确认 / 状态可读 / 风险可读 / 表现打磨中本阶段应达到哪层
后置但保留：本阶段不精修、以后仍必须处理的手感与内容
推进判断：进入验收 / 修改当前切片 / 停止并解决偏离
```

阶段对齐报告之后，AI 再给出与当前切片对应的作者测试清单，固定写明：准备入口、操作步骤、应当看到、不应看到、需要作者判断的体验项、通过标准和完成后停点。技术测试不能代替作者对新玩家行为、手感、数值、美术或故事因果的验收。没有项目适配 Skill 时也必须执行这一格式；Skill 只能帮助生成，不能取代本文件的强制要求。

阶段推进只追求当前目标所需的完成度。R1 先证明动作、状态和工序传递，R2 证明取舍与翻译，R3 证明故事因果，R4 才集中处理完整节奏与表现；不得因局部细节尚未精修而阻塞核心闭环。

### R0：双运行时技术门

目标：先证明 Three.js 方案在浏览器和微信小游戏都能使用同一份状态与渲染逻辑，不把玩法建立在未知兼容性上。

交付：固定斜俯视相机、程序化灯光、铁砧、24 截面钢坯；浏览器点击或微信触摸钢坯后局部截面确定性变化。

验收：浏览器自动测试通过；微信开发者工具可编译显示；作者真机预览可触摸并看到变化；两端回放同一操作得到相同 JSON；微信包体合规；Windows 全新 clone 能重新构建两个产物。

### R1：最小可玩锻造链

目标：跑通选料 -> 加热 -> 锻打 -> 淬火 -> 研磨，先不做工坊总览、故事、回火、正火、装配或正式美术。

交付：两套输入产生同一种意图；局部锤击影响相邻截面；温度、形变、应力、损伤、淬火和研磨状态可见；每次锤击 100ms 内有反馈；同一回放跨平台一致。各工序完成后以一个独立整合切片跑通完整最小链，不把五个功能留成互不传递状态的近景。

验收：作者可连续锻打 30 次不出错；高温与低温、对称与单侧锻打产生肉眼可见且数据可解释的不同；完整最小链能导出一份可评估的最终状态。整链验收同时记录各工序的等待、重复操作、思考时间与有效决策，不先拍脑袋规定整局时长。

### R2：物理规则与隐藏六维

目标：把 R1 的最终状态稳定地翻译成六维、特性和缺陷，证明没有简单“全满神刀”。

交付：数据校验、固定样本、`?debug=1` 作者调试页与自动测试；每个缺陷都有可追踪的状态来源。固定样本至少覆盖高低温、对称与单侧锻打、不同淬火时机、均匀与不均匀研磨等因果对照。实现六维前先用 3 至 5 个故事条件探针检查各维是否能产生独立后果；探针只验证数据接口，不提前制作 R3 内容。

验收：同一操作序列恒定；改变物理规则后标准样本能解释变化；极端/怪异外形仍合法；六维取舍与缺陷符合预期；每一维都能对应至少一种可理解的故事后果；玩家生产界面不显示数字评价。

### R3：最小故事闭环

目标：一把真实锻出的武器能进入一段因果一致的 NPC 冒险。

交付：一个 NPC、三类情境、3 至 5 个确定性事件与结局；故事只读 `WeaponData`。每个关键事件保存触发它的维度、特性或事实缺陷，不用无关文案解释结果。

验收：至少三套不同操作得到明显不同且可解释的故事；薄刃、厚钝、前端过重、弯曲、裂纹等会以合理后果进入故事；同一武器和种子结果一致。

### R4：正式体验与作品集

目标：把已验证的链条做成完整可展示体验。

交付：2.5D 工坊总览、器具入口、反馈打磨、程序化视觉风格、存档与分享数据、试玩记录、演示录像、规则样本与系统设计复盘。完整链成立后再根据实测工序时长、有效决策和玩家困惑调整节奏，不把单纯等待误当作游戏时长。

验收：至少 5 名陌生玩家试玩；4/5 能从故事指出一个自己锻造造成的后果；3/5 第二次锻造会主动调整策略并在实际操作中作出对应改变；怪异武器可进入故事而不是报错或被强行纠正。小样本只用于发现问题和验证机制，不包装成市场统计结论。

### R5：锻造核心产品化与权利准备

目标：在《打了个铁》核心闭环和玩家价值已经得到验证后，判断锻造系统能否作为独立资产复用、授权或合作开发；R5 不反向扩大 MVP。

交付：一个脱离《打了个铁》故事与界面的最小接入样例；经过验证的核心模块边界、公共契约、版本规则、依赖与来源清单、集成说明；针对第二个真实使用场景的差异分析；源码授权、闭源交付、定制开发或联合开发的方案比较。

验收：第二个宿主能在不导入《打了个铁》应用与故事代码的情况下重放操作、读取状态并获得评估结果；核心修改不会破坏原游戏固定样本；作者明确选择是否以及如何对外提供，未获授权前不发布独立包、不承诺商业许可。

## 9. 产品化与产权治理

产权治理的目的，是保存独立创作证据、厘清来源与合作边界，并降低未来授权争议；它不能保证垄断抽象玩法，也不能由 AI 代替专业法律判断。

### 9.1 从现在持续保留的证据

- Git 提交、PR、版本标签、计划变化、规则样本、自动测试、作者验收、试玩录像和演示产物共同形成连续创作记录；不另建重复的流水账文档。
- 代码、规则数据、设计文档、文案、美术、音频和生成内容都要能说明来源、作者、许可与修改情况；权属不清的内容不进入可交付版本。
- 第三方依赖必须锁定版本并记录许可证；未来引入模型、贴图、字体、音效或外部代码时，先确认是否允许商业使用和再分发。
- 公司代码、内部资产、保密文档和权属不清的职务成果不得进入本项目。鉴于项目曾在公司设备上开发，商业合作前由作者核对劳动或实习协议，必要时咨询知识产权专业人士。
- AI 可以辅助实现和整理，但产品目标、关键取舍、作者验收与版本归档必须保留人的决定和可验证证据。

### 9.2 对外动作的三道检查门

| 时机 | 必须确认 | 未确认时的边界 |
| --- | --- | --- |
| 当前开发 | 仓库公开范围、既有公开内容、公司协议与第三方来源风险 | 不因本计划自动修改仓库可见性或删除历史 |
| 对外合作前 | 保密范围、参与者职责、既有成果、共同改进成果、使用地域/期限、再许可和退出方式 | 不只依赖口头约定，不向合作方交付完整核心源码 |
| 稳定发布或授权前 | 软件著作权登记、商标、许可证、商业合同，以及是否存在值得专业评估的专利或不正当竞争保护路径 | AI 只整理材料与风险，不给出法律有效性保证 |

当前 GitHub 仓库为公开仓库且没有 `LICENSE` 文件，这是已知的披露与合作风险，不等于他人当然取得使用许可，也不能阻止他人看到实现。是否转为私有、采用何种许可证或调整公开内容，必须另设检查点，由作者在了解既有公开无法撤回的前提下决定。

## 10. 微信适配止损线

微信与浏览器有不同运行入口，但共享 `GameApplication`、`forge`、`evaluate`、`story` 和 `render`。渲染器显式接收画布与 WebGL 上下文，游戏自行驱动帧循环。

如果同一适配路径出现两次无法解释的失败，或适配修复超过一个完整工作日，必须停止玩法开发并给作者风险报告，不能悄悄扩展成通用微信引擎。

微信开发者工具登录、导入、真机扫码和真机验收由作者完成；AI 的交付是可导入工程、明确操作说明和浏览器侧证据。

## 11. 开发环境与交接

- 当前唯一开发设备为私人 Windows；旧公司 macOS 已完成交接，不再拥有未推送工作。
- Windows 使用 Node `v24.19.0`、npm `11.17.0`。`main` 与火炉 WIP 均已分别通过类型检查、逻辑测试、浏览器/微信构建和对应 Playwright 端到端测试。
- GitHub CLI 已登录；微信开发者工具的安装、登录、导入与真机预览仍由作者操作，完成前不得声称微信真机验收通过。
- 每次离机前运行可用检查、更新本文件、推送已验收提交或明确的 `[WIP]`，并确认本地与远端提交一致。接手设备先读 `AGENTS.md`、本文件和 Git 历史，再执行 `npm ci` 与 `npm run check`。

## 12. 现状、废弃项与下一步

本次重启保留 Git 历史，但当前执行只以现行文件树、`AGENTS.md` 和本文件为准。旧 Cocos 路线、旧文档结构、旧模块编号和旧半成品均已退出现行文件树；它们只作为历史提交保留，不得恢复到新功能中。

已确认的变更只有三项：

1. 引擎从 Cocos Creator 改为 TypeScript + Three.js。
2. 浏览器不再是之后会被丢弃的原型，而是与微信共享代码的持续开发入口。
3. 执行顺序改为先证明双运行时，再做最小锻造链和六维评估，之后才做故事。

`R0.5` 至 `R1h-3a` 的已验收 Three.js 工作通过 PR #9 归档到 `main`。Windows 全新 clone 已能重建浏览器与微信产物并通过全部自动检查；微信真机预览仍需作者操作。当前 `feat/R1i-furnace-heating` 的 HEAD 为 `c2cad52`，已归档架构基线、可回滚锤击 WIP 和第一人称热锻入口，但尚未完成作者体验验收，不能直接视为稳定版本。

## 13. 模型与设备交接摘要

下一位 AI 必须先阅读 `AGENTS.md` 与本文件，并执行 `git status --short --branch`；不得把聊天记录当作项目事实。开始修改前确认本地与远端一致，不直接改 `main`，不重置或清理历史。

当前可试玩入口由 `npm run dev -- --host 127.0.0.1 --port 4177` 启动。已知检查命令是 `npm run typecheck`、`npm run test`、`npm run test:e2e`、`npm run check`。项目依赖安装使用 `npm install`；运行环境为 Node.js 24。微信开发者工具的登录、导入与真机预览只能由作者操作，AI 不得声称已替作者完成。

当前已通过自动验证：浏览器与微信同源入口、第一人称热锻工作站原型、点击/蓄力锤击、局部接触预览、近景铁砧构图、`A/D` 钢坯转面、`W/S` 送料、稳定薄坯局部塑形、基础加热状态和派生接口测试。当前未完成或未获作者体验验收：统一物理模型、第一人称工作站最终表现、氧化皮画面、锤击与操作的完整反馈、选料/切割/焊合/淬火/回火/研磨的独立可玩操作、完整 R1 链、六维、故事和正式美术。旧的“多角度锻打、铁砧棱角/圆锥接触与动力锤不进入 MVP”不再作为长期架构限制；是否进入当前切片以当前架构文档和阶段授权为准。

严格保留的产品共识：这是 2.5D、程序化 Three.js 的真实微信小游戏项目，浏览器仅为同源调试和展示入口；不使用 Cocos、Unity、外部场景编辑器、GLB 或贴图。锻造不是主观扣分，质量和缺陷必须从材料、几何、热处理、完整性/表面等可解释状态推导；最终隐藏六维只用于故事因果，不展示总战力。首版不做有限元、软体物理、真实化学、任意拓扑或运行时 LLM。
