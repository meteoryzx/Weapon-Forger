# 《打了个铁》项目计划

> 本文件只记录当前产品方向、阶段计划和验收状态。协作与 Git 执行规则只以根目录 `AGENTS.md` 为准。

## 0. 唯一当前检查点

- **编号**：`R1-B1`
- **名称**：悬臂圆锯切割工位
- **状态**：作者于 2026-09-11 明确确认“体验通过，我很满意，可以进入下一步”；切割已验收，进入 Git 归档。
- **稳定基线**：`main@f752faf`
- **工作分支**：`feat/R1-saw-interaction`
- **目标**：玩家在同一工坊的悬臂圆锯下自由摆放与旋转金属（允许伸出桌边），看到有限切割预览；未切断则保留真实缺口，产生独立连通部分时才新增可分别加工的工件。
- **本次范围**：后沿悬臂圆锯模型、桌面移动与平面旋转、有限刀路虚线、共用核心的预览与确认执行、产物分别选取与继续加工；修改 app/render/entry、index.html、必要规则与测试，沿用同一工坊坐标。作者本轮明确要求取消摆放边界与必定二分限制：扩展 forge 占据几何、连通判定、外表面提取及对应文档与回归，保留每个有限切口的材料与过程状态。
- **明确不做**：折叠、扭转、倒扣或完整三维布尔引擎；不重写热力学与宿主数值；不修改 DeepSeek 工作区；正式音效美术后置。有限锯缝自然形成的槽口纳入本次，独立钻孔工具与三维局部切深不在范围内。
- **自动检查**：治理、类型、单元测试、浏览器与微信构建、Playwright；摆放不改材料、世界刀路与局部坐标互逆、后台预览与确认采用同一规则结果、过期结果不能提交、材料与锯缝守恒、产物同尺度接触桌面及后续加工路径通过。
- **作者验收**：已通过移动与旋转、锯片朝向、桌面接触、有限预览、缺口/分件、90 mm 行程和同一工坊空间关系；不宣称微信真机验收。
- **Git 处理**：按作者已授权的 Git 流程完成提交、推送、PR、CI 核对、合并及短期分支清理；归档范围仅包含已验收的 R1-B1。
- **完成停点**：切割归档后进入下一项焊合的共识梳理与参考图阶段；沿用“先 grill、再参考图提示词、图确认后施工”的顺序，不提前改焊合实现。
- **实现证据**：本轮 `npm run check`（15 个测试文件、97 项单测，含治理/类型/Web 与微信构建）、`npm run test:e2e`（17 项浏览器路径）与 `git diff --check` 通过。实测 90° 越桌旋转、有限内部槽口仍保持一块；浏览器回归覆盖伸出桌边、先留缺口再补切、工件身份与姿态不跳变、材料与损耗守恒。构建仍有原有大包体提示；微信真机未验证。
- **中心末端修订**：末端锚定桌面中心，预览/动画/执行共享端点，默认材料位于中心稍后方；作者认为 60 mm 总行程偏短，本轮只将靠机器的一端向底座延伸，使总行程增至 90 mm。规则位于 `src/forge/forge-rules.ts`，`src/app/cut-placement.ts` 自动派生刀路端点，默认位置不变。本轮 `npm run check`（97 项单测及两平台构建）和两条切割浏览器回归通过，验证预览/确认、独立取用及半切后补切；画面见 `output/playwright/cut-guide-extended-rear.png`。此前完整 17 项浏览器回归通过，本轮仅重跑受参数影响的切割路径。
- **空间连续性约束**：选料、切割、锻打、热处理等工位始终属于同一间连续工坊；参考图只约束工位造型、观察角度和大体方位，不得据此拆成彼此独立的场景。后续近景操作通过同一空间内的关注点切换实现，并以当前项目布局为准。

## 1. 项目目标

《打了个铁》是一款模拟锻造过程的游戏。玩家获得的是一组可以自由组合的锻造操作，而不是按固定配方点击按钮领奖励。玩家应能选择和组合材料，控制温度、切分、焊合、塑形、热处理和表面加工，并让结果从操作与材料状态中自然产生。

### 世界观与视觉共识

- 故事发生在低魔法世界，时代处于早期机械化阶段：技术明显超过纯手工时代，但不是现代工业社会。
- 工坊可以拥有性能较强的设备，动力可来自水力、蒸汽、齿轮、皮带、配重或低魔法；设备外观应像当地工匠能够制造、维修和改造的铸铁、木材与铜件结构。
- 避免现代电机、电线、塑料外壳、数控屏幕、现代工业流水线和科幻设备。
- 八个工位属于同一个连续工坊。参考图只切换观察位置和当前工作台，不重新创造割裂的房间或风格。
- R1 使用低集合体和简单几何体，优先表达位置、比例、接触、朝向和操作关系。精细模型、材质、动画、音效、镜头和特效后置。
- 参考图是空间与功能草图，不是最终美术质量目标；几何关系错误时，优先修正几何关系。

项目有两个长期交付物：

1. 一款以锻造为核心玩法的游戏。
2. 一套边界清楚、可测试、可迁移到其他游戏或合作项目中的锻造核心。

未来游戏可使用 2.5D 工坊作为中枢、第一人称近景作为具体操作场景。低魔法、符文、宝石等虚构能力应通过扩展数据与工艺接入，不能写死在基础物理或某一套武器属性里。

## 2. 当前产品共识

- R1 只证明“可迁移的锻造交互基础成立”，不宣称完整玩法已经成立。
- 八类操作是当前最小能力集合，不是永远不可改变的八步固定流程。后续优先扩充现有能力；只有出现无法表达的基本操作时，才新增基础动词。
- 玩家可以自由决定操作顺序；系统负责判断操作是否可执行并产生一致后果。
- 所有操作作用于共享工件状态。材料、几何、温度、应力、损伤和工艺历史必须互相影响，不能成为八套互不相干的加减分。
- 核心层交付原始事实 `ForgeFacts`，不内置攻击力、破甲、六维、故事结论或总评分。不同游戏在下游用自己的公式消费这些事实。
- 真实感采用可解释的降阶模拟，不宣称工业材料仿真。当前优先保证因果正确、确定性、可保存和可扩展。
- 画面在 R1 只需让操作对象、输入和结果可辨认；精细模型、镜头、动画、音效和美术统一后置。

## 3. 已确认开发顺序

1. 恢复干净的八类操作验收基线，并删除会误导开发的旧内容。
2. 作者逐项体验和调整八类操作；每项必须单独通过。
3. 八项通过后进行完整工艺链回归，确认状态能连续传递。
4. 做一个很窄的下游消费样例，验证 `ForgeFacts` 足以计算有意义的武器属性；样例不是固定产品规则。
5. 基础与消费接口成立后，再集中打磨手感、画面、动画和音效。
6. 工坊中枢、订单、冒险故事、魔法扩展和商业化内容在后续检查点另行批准。

## 4. R1 八项验收状态

本次选料确认：候选材料是可重复领取的来源；查看和取消不创建工件；确认才创建固定规格的独立工件。单项选料从空料架开始。料架每页展示四件，但不限制核心总工件数，切割产物不因显示容量丢失。工件选择保留原材料、温度、几何与工艺历史。参考画面确定自然木桌、无手部、冷暖光与克制金属色，不把亮度作为品质评分。

| 操作 | 当前基础能力 | 作者状态 |
| --- | --- | --- |
| 选料 | 固定镜头中从料架抽出查看、确认后进入四格暂存桌、按身份直接取用或放回、携带原工件前往已有工位 | 已通过 |
| 切割 | 同一工坊内自由摆放/旋转、有限圆锯预览与确认；保留缺口，断开连接后独立选取组件继续加工 | 已通过（2026-09-11） |
| 焊合 | 选择两个工件并形成接头，材料与层数按状态计算 | 待验收 |
| 加热 | 工件进出热源，温度与热历史随时间变化 | 待验收 |
| 锤击 | 指定落点与力度，局部几何、应力和损伤按状态变化 | 待验收 |
| 淬火 | 将热工件置入介质，记录冷却条件与工艺事实 | 待验收 |
| 回火 | 控制回火温度并记录可供后续消费的状态 | 待验收 |
| 研磨 | 通过连续轨迹移除材料并记录覆盖与均匀程度 | 待验收 |

八项的代码存在不等于验收通过。自动测试证明规则和构建没有明显破坏；操作是否清楚、是否像在锻造、反馈是否足够，由作者实际体验决定。

## 5. R1 通过标准

- 每一项都有独立操作场景和实际输入，不以一组文字按钮代替操作。
- 左侧中控台可直接进入任意一项，方便作者反复验收。
- 可以明确选择当前工件；切割产生的新工件和焊合对象不会混淆。
- 八项都写入同一个 `ForgeState`，并统一通过 `applyForgeIntent` 进入规则层。
- 相同初始状态与相同操作记录得到相同结果，可保存、加载和回放。
- 几何、材料和工艺只输出原始事实；下游解释与基础系统分离。
- 平衡参数集中管理，公式与测试能解释关键因果。
- 浏览器版本供作者体验；微信当前只要求构建通过，真机体验由作者完成。

## 6. 架构边界

```text
浏览器 / 微信输入
        -> ForgeIntent
        -> 规则校验与 ForgeOperation
        -> ForgeState
             |-> ForgeSnapshot -> 画面
             `-> ForgeFacts    -> 未来的游戏专用消费公式
```

- `src/forge` 是可迁移核心，不依赖 Three.js、浏览器或微信接口。
- `src/render` 只读取快照表现结果，不反向改写规则状态。
- `src/entry` 把平台输入翻译成意图，不在界面代码里偷偷计算锻造结果。
- 游戏专用评价、故事、订单和魔法规则位于未来的下游模块，不污染锻造核心。

## 7. 当前不承诺的内容

- 工业级有限元、完整冶金相变或真实化学仿真。
- 任意三维拓扑、独立开孔工具、折叠和完整自由建模；有限锯缝自然形成的缺口、内部槽口和独立组件属于当前切割范围。
- 固定六维、总战力或故事是唯一消费方式。
- 正式工坊、美术资源、音画反馈和商业发行完整度。
- 八类操作之外的装配、镶嵌、铭文、正火、退火、动力锤等扩展工艺。

这些内容只有在 R1 基础验收后，经过新的检查点说明和作者批准才进入开发。

## 8. 证据与状态更新

### 当前切割交付审查（R1-B1）

- 当前阶段与检查点：R1-B1 悬臂圆锯切割工位，作者已确认体验通过，进入归档。
- 计划目标：摆放、平面旋转、有限切割预览、确认切割、独立取用产物。
- 实际完成：玩家可拖动金属，使用滑杆、Q/E 或 Shift 拖动旋转，允许伸出桌边；有限虚线随表面显示，提示预计仍为一块或分为多块；确认后留下真实缺口，只在断开连接后把新组件放入后沿暂存区。
- 可用证据：17 项浏览器回归通过，覆盖越界摆放、未切断后补切、身份/姿态保持及材料损耗；几何回归覆盖内部槽口、斜向缺口、一刀产生四个组件及后续加热/锤击/研磨/保存/再次切割；截图 `output/playwright/cut-overhang-preview.png` 与 `cut-slot-retained.png`。最终完整检查结果见本文件顶部。
- 尚未验证：微信真机；本轮浏览器玩家体验已由作者明确验收。
- 偏差：scope（作者明确要求的修订）——取消原“完整留在桌面、每刀二分”限制，按真实剩余连通组件产生工件；其他已经认可的布局与操作沿用此前 B1。技术适配扩展到非一致细分面的连通判定和外表面提取，没有重写热力学或宿主数值。
- 当前需要的体验精度：state readability（状态可读），包括同尺度产物、明确损耗和有限刀路有效性。
- 后置保留：精细模型、音效、最终材质、完整复杂接缝力学；现有八项与宿主事实接口保留。
- 推进决定：作者体验通过，归档 R1-B1；下一项为焊合的共识与示意图，具体交互在 grill 后确定。

因果链：玩家以位置和旋转选择切口，允许越过桌边；有限行程决定本刀实际移除的材料及剩余连接，未切到的部分不会自动切断；预览后台执行同一个核心意图，未确认时不改变工件；确认仅提交与当前源状态和摆放相符的结果；仍连通则维持身份与姿态，分离后各组件继承材料状态，损耗单独记账，后续工位读取真实边界。动画只表现该结果，不另算切割体积。

本轮行为：玩家可越过桌边调整材料，让有限锯缝逐步改变真实形体，并在连接断开后得到可分别继续加工的组件。

| 因果环节 | 本轮证据 |
| --- | --- |
| 玩家目的 | 选择缺口位置或逐步分离材料。 |
| 操作 | 平移、旋转、查看预览、确认后再移动补切。 |
| 约束 | 刀路行程和 1 mm 锯缝决定实际接触；空刀不执行。 |
| 信息 | 预览明确显示仍为一块或分成几块，以及预计损耗。 |
| 持久状态 | 占据实体、连通组件、材料/累计功分配、切割记录和损耗账目可序列化。 |
| 即时反馈 | 切口真实可见；提示连接状态，独立组件进入暂存。 |
| 可读差异 | 短刀路只形成缺口，补切连接才分件；浏览器验证两种结果。 |
| 下游影响 | 斜缺口后加热、锤击、研磨、保存及再次切割回归通过。 |
| 重复决策 | 可改变后续刀路，既有空隙不重复扣材料。 |

体验准备：打开 `http://127.0.0.1:4181/?accept=cut`，固定样本为一块冷弹簧钢。左侧步骤按钮会重置为独立样本；验证同一工件流转使用“工坊总览”后点击实际工位。

1. 拖动金属、调节旋转至 90°，允许材料伸出桌边。预计材料总量与切割次数不变；等待检查后可形成槽口的刀路为绿色，只有完全未接触剩余材料的刀路才提示调整。
2. 点击“确认切割”。预计只执行一次，当前工件变短，另一块以相同比例接触后沿桌面；默认直切总量 129.024 cm³ = 两块保留材料合计 128.640 cm³ + 锯缝损耗 0.384 cm³。
3. 点击后沿工件，或使用“加工对象”选择，重新摆放后再次切割。预计原工件进入暂存、身份不混淆、材料不丢失；暂存一页显示一块以保持真实比例，通过翻页访问全部工件。
4. 重置后聚焦画布、按五次向下方向键（或拖到桌面中心），此时虚线末端位于材料宽度中间。预计可以确认并形成半宽缺口，数量仍为一块，位置与旋转不跳变；再向后移动让刀路切过剩余连接，预计才出现可独立选择的产物，既有缺口不重复扣除材料。无需把材料拖近桌前沿来做半切。
5. “工坊总览”查看选料、切割、锻造和热处理的相对方位。进入其他工位应保留当前工件状态；R 或“重置当前样本”才清空本体验会话。

不得出现：移动即切断、虚线与切口方向不一致、桌面穿模、产物比例缩小导致误解、重复确认多切一次、后续工位因切后原点而点击落空。

作者于 2026-09-11 确认上述切割体验通过；代码与公共接口技术审查、Git 归档由 AI 负责。当前选料和切割两项已通过，焊合等六项仍待各自验收。

Git 记录：前置修复 PR #17 已 squash 合并为 `f752faf`，旧 PR #16 已关闭；本次 B1 在 `feat/R1-saw-interaction` 独立归档，提交与合并事实以对应 PR 和 Git 历史为准。DeepSeek 工作区未修改。

### 当前选料交付审查

- 当前阶段与检查点：R1-A，八项中的选料；作者已于 2026-09-10 确认体验通过。
- 计划目标：把材料查看、确认领取、保留工件和再取用接成可独立体验的过程。
- 实际实现：三种钢料沿画面深度平放并接触料架；点击后先抽出层板再向玩家下倾，不发生横向翻滚；确认后在同一镜头的桌面每页平放四件，可按身份直接选择或放回料架。离开选料前要求当前工件位于桌面，避免镜头切换改变其所属位置。
- 自动证据：`npm run check` 通过，60 个单元测试通过，类型、治理、网页与微信构建通过。测试覆盖查看取消、重复领取、工件取回、分页不丢件、固定规格、三态轴向、层板与桌面接触、四件桌面陈列及加工历史守恒。构建仍提示网页单包超过 500 kB，本轮不做拆包优化。
- 未验证：移动设备与微信真机；没有运行 Playwright，浏览器体验结论来自作者亲自验收。
- 偏差：根据作者体验反馈取消料架辅助镜头，改为同一固定镜头直接选择桌面或已放回工件；未改变锻造核心接口和其他七项工序。
- 当前体验结论：作者确认选料环节通过；不宣称正式美术品质完成。
- 后置保留：既有热力学与形变架构、其他七项操作、正式模型与动画音效打磨。
- 推进结论：归档 R1-A；PR 合并后另行提出 R1-B 切割检查点，不自动施工。

材料选择的因果链：玩家点击架上钢料获取材料事实，抽出与倾斜反馈只改变画面；确认才创建独立原始工件并放到暂存桌；桌面或放回料架的工件按身份切换，保留材料和加工数据；后续热与力学公式继续读取这些材料参数。当前材料来源无限、无费用，反复领取没有资源惩罚；是否有策略取舍不属于本次证明范围。

### 作者选料体验清单

准备：打开 `http://127.0.0.1:4178/?accept=materials` 并重置，初始暂存桌为空、没有当前工件，三种材料来源平放在后方料架。

1. 分别点击架上三种钢料。预期对应钢料先平移抽出层板，再向玩家下倾，信息显示材料名称、碳含量、固定规格与质量；点击“放回”后暂存桌仍为空。
2. 选一块并“确认领取”。预期镜头不切换，新增工件沿画面深度平放在桌面；首次领取成为当前工件，后续领取不暗中替换当前加工对象。
3. 重复领取到五件。预期同一桌面出现分页，第一页四件、第二页一件；翻页不能增加或丢失工件。
4. 点击桌面不同钢料。预期当前材料名称与工件身份对应改变；相同材料的不同工件仍可分别取用。
5. 将当前工件“放回料架”。预期同一镜头内回到架上，携带入口禁用；点击该工件取回桌面后才可前往切割台或火炉。加工后再返回选料，材料、温度、形状和历史保持，不能恢复成一块新的冷钢料。

注意：左侧八项中控台切换的是独立重置样本；验证同一工件流转请使用底部入口。刷新或重置会清空当前体验会话，本轮未做浏览器存档。

作者结论：上述选料画面与流程已经作者亲自体验并明确确认通过。当前进入归档 PR，不自动开始切割；R1-B 仍需另行说明和批准。

- 自动检查结果只证明代码层事实，不能代替作者体验验收。
- 每完成一个检查点，都必须在本节或当前检查点中记录实际完成项、未验证项和下一步。
- `main` 只保存作者已经批准合并的稳定结果；未验收工作保存在说明清楚的功能分支。
- 历史提交用于追溯，不是当前指令。任何 AI 接手时只读取根目录 `AGENTS.md` 的协作规则和本文件的现行计划。
