# 《打了个铁》项目计划与交接

> 本文件保存当前有效的产品设计、工程边界、阶段状态和跨设备交接信息。它替代旧的分散交接文档；旧 Git 历史仍保留，但不再作为当前执行依据。

## 当前执行补充：可迁移锻造核心

当前产品优先级固定为：八项操作逐项验收 → 完整链回归 → R1 归档 → 一个窄下游契约探针 → 集中优化手感、美术和音效。六维、故事或动作游戏属性都不能在探针前被写成既定产品方案。

`src/forge/index.ts` 是可迁移锻造核心的公共入口。核心不得依赖 `app`、`render`、`entry`、`platform`、Three.js、浏览器、微信、故事文本或某个游戏的专有属性。动作游戏的攻击力/破甲/防御，冒险游戏的六维和故事条件，或魔法系统的奥术亲和/符文承载，都必须通过下游 profile 消费 `ForgeFacts`，不能反向污染 `ForgeState`。

当前仓库只交付原始状态、快照、事实出口和可扩展派生接口。仓库里已有的参考 profile 只用于验证接口，不代表六维、故事或任何正式下游设计已经通过。

## 当前架构决策（2026-08-21）

本项目下一阶段不是继续增加独立按钮或各工序的局部加减规则，而是验证一套统一的锻造过程模型：八个工艺类别共享几何、质量、温度、应力、塑性、材料组成、损伤、表面和界面状态；不同工艺只提供不同的工具接触、运动和热边界。

统一模型的研究基线见 `docs/FORGE_UNIFIED_PHYSICS.md`，代码分层、文件所有权、公共契约和交接规则见 `docs/ARCHITECTURE.md`，核心 API 见 `docs/FORGE_CORE_API.md`。这三份文档是设计讨论和后续实现的入口，不代表其中所有公式已经实现。

### 当前模型重构检查点（2026-08-21）

在继续制作切割、焊合、淬火、回火和研磨的独立操作前，先完成统一降阶物理模型的第一轮落地。当前 `physics-2` 已把材料屈服强度、热软化、等效对数应变、弹塑分配、残余应力、损伤累积和机械功接入同一纯逻辑模块；现有 XPBD 点阵继续负责节点位置、砧面支撑和体积约束。冷态锤击的风险不再由命中次数或固定 `+damage` 驱动，而由弹性载荷、局部化、薄截面和支撑状态共同产生。该段记录的是模型落地时的历史检查点；八工位操作随后已在此底座上完成浏览器 MVP 接入。

本检查点只证明统一响应链和锤击回归，不宣称完整材料仿真。八工位随后已接入同一状态，但除锤击外仍主要是降阶过程模型；后续只能依据固定样本逐步提高共享模型精度。任何新工序不得绕过该响应层直接写入损伤或质量。

### 当前执行纪律：先做完整 MVP，再集中打磨（2026-08-21）

本阶段不再继续反复重构模型后才开始做内容，而是以当前 `physics-2` 为底座，先完成八个锻造动词的最小可玩纵切片：选料、切割、焊合、加热、锤击、淬火、回火、研磨都必须能被玩家实际操作，产生可见结果，并把结果写入同一份可序列化原始状态。

- MVP 允许使用简化的场景、器具模型、动画和反馈，但不允许删除或绕过 `ForgeIntent`、`ForgeOperation`、`ForgeState`、`ForgeSnapshot`、`ForgeFacts` 或 `physics-2` / XPBD 底层。
- 物理架构是后续打磨的基础：打磨阶段可以校准材料参数、载荷、温度、几何响应和呈现，但不能把统一模型退化成各工序独立的固定加减。
- 每个动词的完成标准是“输入可执行 + 状态有因果变化 + 画面能读出变化 + 有固定样本自动测试”，不是增加一个按钮或一条 HUD 文案。
- 试玩中发现但不阻塞八动词闭环的问题，统一登记到 MVP 验收后的精修批次；不得在闭环完成前因局部观感反复触发整套架构重写。
- 本 MVP 不实现 `evaluate`、`story`、正式美术、音效和微信真机验收；`npm run check` 只负责自动契约和构建，真实感与可玩性由作者通过八个固定操作样本逐项验收。

因此，当前顺序是：完成基础逻辑纠偏 → 八项操作逐一体验和验收 → 完整链回归并归档 R1 → 一个窄下游契约探针 → 集中打磨物理、手感和画面。

### 当前优先级

1. 校正仓库事实和交接入口，避免把 WIP 写成已验收功能。
2. 以当前 `physics-2` 和 XPBD 为底座，先完成八个动词的实际可操作 MVP 纵切片。
3. 为每个动词补齐输入、状态变化、最小可读画面和固定样本测试，不为每个工艺创建孤立的质量分数。
4. 用八个固定样本逐项记录真实问题；单项通过后再进入下一项，八项全部通过后才校准跨工序节奏和整体表现。
5. R1 归档后先做一个窄下游契约探针；通过后才进入集中精修。六维和故事仍不提前写回锻造核心。

### 当前明确不宣称

- 当前分支不是已验收 R1 完整链；
- 当前画面是已通过自动检查的第一人称八工位锻造 MVP，尚未完成作者体验验收和最终表现打磨；
- 当前点阵/XPBD 不是完整材料仿真；
- 当前损伤、焊合、切割和研磨仍需按统一模型继续校正；
- `npm run check` 全绿只能证明功能契约稳定，不能代替作者对真实感、可读性、模型和镜头手感的验收；本轮模型和镜头均为可替换占位。

### MVP 交互重做验收结论（2026-08-22，自动验收通过，等待作者体验验收）

上一提交虽然让八个 `ForgeIntent` 都能被浏览器触发，也通过了自动测试，但实际体验仍是“多个工位方块 + 点击后直接提交意图”。本轮已重做为总览到工位近景的连续输入切片，并通过固定视口端到端回归；视觉模型仍是简化占位，等待作者体验验收和后续调整。

下一版 MVP 的硬验收条件：

- 点击其他工位后必须有相机移动/过渡，并对构图、俯视角和工作距离做工位级微调；不能只是 HUD 标题变化。
- 每个工位必须在自己的镜头里显示当前工件、工具和作用区域；工具方块只能作为临时占位，不能替代作用区域。
- 每个动词必须有独立的连续输入过程：选料要从材料堆中点选，切割要沿切口拖动，焊合要把两块工件靠拢，炉内加热要按住并观察温度，淬火要拖入介质，回火要调整温度后送入，研磨要沿刃口连续移动，锤击继续沿用落点/蓄力/转面/送料。
- 操作完成前不能直接写入结果；玩家的输入过程必须能被中断、改变或重复，并在快照中留下对应事实。
- 自动测试必须至少验证一次相机工位变化和一次连续输入事件；不能只点击工位方块后断言操作计数。

上述自动条件已满足。项目进入“八项操作体验验收”停点：作者通过左侧中控台逐项验证八个工位；每项可以单独调教和重置，八项全部通过后才执行完整链回归并归档 R1。视觉构图、模型和相机细节只在阻碍当前操作判断时修改。

## 0. 当前模式：诊断、批准、施工、八项验收

- **工作模式**：作者反馈授权只读诊断和测试；AI 先报告问题性质、修改范围和验收方法，作者批准后在该范围内自主施工。当前进入八项操作验收，每次只处理一个固定样本。细则见 `AGENTS.md` 第 2 节。
- **正式基线**：`main` 的 `2cb279f` 是已归档的早期稳定基线；当前八工位 Demo 工作集中在 `feat/R1i-furnace-heating`。
- **接手状态**：Windows 已安装 Node 24、依赖和 Playwright Chromium。本地与远端关系及未提交工作只能以当前 Git 检查结果为准，不能沿用文档中的旧 HEAD。作者批准归档前，AI 不提交、不推送、不合并。

### R1 目标与范围

**目标**：玩家能分别使用选料、切割、焊合、加热、锤击、淬火、回火和研磨处理金属工件，并在八项通过后走完组合链，产出可序列化的原始状态。R1 不要求产出完整武器，不包含正式下游属性或故事。

**本次做**：

| 工序 | 内容 | 状态 |
| --- | --- | --- |
| 选料 | 2 种材料（低碳钢/高碳钢），可硬化潜力、韧性、重量有可感差异 | 新增 |
| 加热 | 沿用火炉（整块均温、送入/取出/热色） | 已有，并入 |
| 锻打 | 沿用锤击形变（转面/送料/蓄力/局部形变） | 已有，并入 |
| 淬火 | 水/油两介质；保存介质、起始温度与冷却结果 | 已实现，待作者验收 |
| 研磨 | 沿刃口滑动；记录覆盖、均匀度与去除体积 | 已实现，待作者验收 |
| 回火 | 保存有序回火温度事件，后续材料模型可读取 | 已实现，待作者验收 |
| 切割/焊合 | 切割一分为二；焊合保留材料区域、界面和工艺历史 | 已实现，待作者验收 |
| 编排 | 八项独立入口与后续完整链回归 | 独立入口已实现，完整链待八项通过 |

**后置（不在 R1）**：正式下游属性、故事、魔法能力和完整武器装配。R1 后只先做一个窄契约探针，再决定具体消费设计。

**本次不做**：微信真机验收（只保证 `build:wechat` 通过，真机由作者做）、微信等价八工位入口、局部分段温差、氧化皮画面、火花/音效精修、存档/分享、正式美术、正火、复杂焊接、装罐/脱罐、装配和服务端防篡改。

**八项验收（Demo 完成后）**：

- 机械项（AI 自证）：`npm run check` 全绿——typecheck + 单测 + e2e + 浏览器/微信构建。
- 体验项（作者逐项判断）：八个固定样本分别达到“操作成立、状态变化可读、没有明显错误”；每项通过后写入台账。
- 整链项（八项通过后）：能连续处理同一金属工件不出错；至少两种操作路线产生可观察且数据可解释的差异；生产过程不显示宿主属性或「优秀/失败」总评。

**完成后停点**：八项全部通过前，AI 每次停在当前操作等待作者结论；八项通过后执行完整链回归，作者明确确认“R1 基础操作验收通过”才归档。

### R1 独立操作验收台（2026-09-07）

启动 `npm run dev -- --host 127.0.0.1 --port 4177` 后，直接打开 `http://127.0.0.1:4177/`。画面左侧中控台列出八个步骤；点击任一项会加载该步骤的固定初始样本并进入对应工位，点击“重置当前样本”或按 `R` 可重新开始。根入口仍显示工坊总览，但当前 R1 验收阶段保留左侧中控台供作者随时进入单项样本。

| 顺序 | 操作 | 固定样本 | 状态 |
| --- | --- | --- | --- |
| 1 | 选料 | 弹簧钢当前工件 + 三种材料堆 | 待作者验收 |
| 2 | 切割 | 未加工弹簧钢钢坯 | 待作者验收 |
| 3 | 焊合 | 已预热的高碳钢当前工件 + 已预热的弹簧钢工作台工件 | 待作者验收 |
| 4 | 加热 | 室温弹簧钢钢坯 | 待作者验收 |
| 5 | 锤击 | 已预热并取出的弹簧钢钢坯 | 待作者验收 |
| 6 | 淬火 | 已预热并取出的弹簧钢；中控台可切换水/油 | 待作者验收 |
| 7 | 回火 | 已完成水淬的弹簧钢 | 待作者验收 |
| 8 | 研磨 | 未研磨弹簧钢钢坯 | 待作者验收 |

单项循环固定为：作者选择一个步骤并体验 → 报告实际表现和期望表现 → AI 先判断是逻辑错误、状态不可读还是后置表现问题 → 只修改当前切片并运行匹配测试 → 作者明确确认该项通过 → 更新本表。一个操作通过不代表 R1 通过；只有八项全部通过并完成整链回归，才能记录 R1 基础操作验收通过。

### 八项通过后的完整链回归清单（原 Demo 清单，2026-08-22）

**准备**

- 本清单当前不执行。只有上方八项台账全部通过后，才打开 `http://127.0.0.1:4177/`，确认首屏是“铁匠铺 · 总览”，画面中没有 HTML 按钮菜单。
- 本清单只验收浏览器 Demo 的玩家行为、状态因果和可读性；微信真机、正式模型、正式镜头、动画、音效和美术不在本次作者验收内。
- 每次进入新工位后等待镜头稳定；用 `Esc` 返回总览，再进入下一个工位。不要在镜头移动中操作。

**基础入口与锤击**

1. 点击铁砧进入“铁砧 · 锤击”，确认镜头从总览移动到铁砧近景，且当前钢坯放在铁砧上而不是与铁砧重叠。
2. 在钢坯不同位置按住鼠标后松开，确认钢坯外形发生局部变化，HUD 的“锤击”计数和原始状态发生变化。
3. 重复按住几次，确认操作不是点击文字或按钮直接提交；用 `A/D` 转面、`W/S` 送料，确认钢坯方向和位置变化。

**八个工位逐项验收**

| 工位 | 操作 | 应看到 | 不通过表现 |
| --- | --- | --- | --- |
| 材料架 · 选料 | 从总览进入材料架，点击材料堆中的一块原料 | 当前工件/材料改变，工作台增加一块，HUD 记录选料 | 只切换标题；材料被遮住或点不到 |
| 切割台 · 切割 | 进入切割台，从当前钢坯表面连续拖过切口后松开 | 钢坯被分成前后两块，工作台数量增加，切割计数增加 | 点击任意空白就完成；只增加计数而无分离状态 |
| 焊合台 · 焊合 | 确保工作台有第二块工件，拖动明确选中的两块工件完成靠拢 | 被选中的两块合并；工作台移除正确对象；材料区域、界面、工艺历史保留，工件级碳含量摘要按体积变化 | 焊错对象；点击工位立即焊合；没有第二块工件也能焊合 |
| 火炉 · 加热 | 按住炉口中的钢坯一段时间，再松开取出 | 热色和温度随按住时间变化，松开后记录加热过程 | 点击炉子立即升温；按住没有温度/热色变化 |
| 铁砧 · 锤击 | 按住钢坯，在不同落点松开；配合转面和送料 | 产生局部形变，位置、方向和锤击数据改变 | 锤子动画与点击无关；只显示固定数字 |
| 水槽/油槽 · 淬火 | 从当前钢坯连续拖入水面或油面后松开 | 对应介质写入淬火状态，温度快速下降，淬火计数/状态改变 | 点击水槽或油槽立即完成；水油没有区别 |
| 回火炉 · 回火 | 拖动温度控制，再松开提交当前温度 | 控制值可连续调整，HUD 记录 `temper.temperatureC` | 点击一次固定写入；没有可调整过程 |
| 磨石 · 研磨 | 沿钢坯刃口连续拖动后松开 | 研磨覆盖率/外形发生变化，研磨计数增加 | 只点击就完成；拖动长度与结果无关 |

**整体通过标准**

- 八个工位都能从总览进入独立近景，镜头会移动，且每个近景能看见当前工件和对应工具/作用区域。
- 八个操作都必须由玩家的点击、按住、拖动或调整完成；不存在“点击工位立即提交结果”的旧按钮机制。
- 至少完成一条链：选料 → 加热 → 锤击 → 淬火 → 回火 → 研磨；另完成一次切割 → 焊合，确认对象、材料区域、界面和工件级材料摘要变化可解释。
- 同一操作重复或改变落点、拖动距离、加热时间、回火温度、淬火介质后，原始状态应出现对应差异；不要求本轮判断数值是否已经真实到最终产品级。
- 不应出现六维评估、故事文本、优秀/失败总评或其他 R2/R3 消费层内容。

**作者需要拍板的体验项**

- 第一人称站在工位前的感觉是否成立，尤其是铁砧、火炉、切割台和淬火台的工作距离与俯视程度。
- 每个动作是否足以让玩家理解“我正在对工件做什么”，而不是在操作占位模型。
- 形变、热色、分离、焊合、淬火、研磨的可见反馈是否达到下一轮精修的起点。

**验收结果与停点**

- 通过：记录八项作者结论，完成整链回归；作者确认 R1 后归档，再做一个窄下游契约探针。
- 不通过：只记录具体工位、操作、实际表现和期望表现；不在没有记录的情况下整体重构物理模型或扩展新工序。

## 1. 项目定位与交付边界

**一句话定位**：玩家以真实操作自由加工金属工件；同一套可解释的锻造事实可以被《打了个铁》或其他游戏按各自规则消费。

这是系统策划秋招作品集。第一优先级始终是“设计深度 + 一条完整、可玩的核心闭环”，不是商业化完整度。

锻造规则核心同时是一项可长期积累的系统资产：在不牺牲当前游戏、不提前建设通用框架的前提下，`forge` 与事实出口保持源码层可迁移；当 R1 和第二个真实使用场景证明价值后，再决定独立打包、授权或合作开发。可迁移性是现在遵守的边界，可售产品化不是 R1 承诺。

已确认卖点与待验证方向：

1. 自由、可重复、能看见因果的锻造过程：玩家敲在哪里、何时加热、如何淬火、如何研磨，钢坯就以对应方式变化。
2. 同一锻造事实可被不同游戏转译成有意义的后果；冒险故事是候选消费方式，不是当前锁定的唯一反馈。

本轮最终产物必须能在**浏览器 H5**和**微信小游戏**运行；它们共享游戏逻辑、Three.js 场景、程序化模型、视觉参数和数据。浏览器是调试、自动验收与作品集入口，不是未来迁移前的临时原型。无需正式上线微信、支付、广告或服务端。

## 2. 玩家真实游玩流程

长期候选体验是 2.5D 俯视工坊中枢与第一人称工作站切换。R1 当前只验收八个第一人称操作工作站；具体镜头、人物呈现和最终工坊布局由作者后续拍板。

```text
进入工坊
-> 点击材料架：选择材料
-> 点击熔炉：加热并观察热色
-> 点击铁砧：钢坯置于铁砧面上，玩家令其转面并用锤子落锤塑形
-> 点击淬火槽：拖动钢坯完成淬火
-> 点击磨石：沿刃口研磨
-> 得到一份自由形状、带原始工艺事实的金属工件
-> 由后续宿主规则解释成武器属性、订单结果、冒险后果或其他用途
```

R1 用左侧验收中控台分别进入选料、切割、焊合、加热、锤击、淬火、回火和研磨的程序化近景。完整 2.5D 工坊中枢属于后续正式体验阶段。

锻打近景的当前核心构图：铁砧居中、钢坯斜放于砧面、锤子在右上方；已确认移除钳子，钢坯须原地围绕自身长轴转面。PC 当前可鼠标短按轻击、长按蓄力、`A/D` 每次转面 90 度、`W/S` 沿钢坯长轴送料；当前不显示独立的白色落点/瞄准特效，避免转面后出现脱离钢坯表面的提示物。瞄准可读性、操作确认、手感反馈和正式 UI 统一后置到体验精修阶段。微信当前有明确转面控件，拖动送料后续实现。两端输入都只能生成相同的 `ForgeIntent`，不能直接写游戏状态。

## 3. 设计原则：自由，但不黑箱

锻造以操作为地基、工艺为配方；下游以 `ForgeFacts` 为接口、宿主规则为配方。系统要让玩家组合出多样结果，而不是让玩家寻找唯一标准答案。

- **工作台状态机，不是固定流水线**：材料对象持有当前状态；工具只在状态合法时亮起。以后新增工艺，只增加操作和门槛，不推翻地基。
- **透明的自由**：自由不是瞎试。温度色、局部形变、裂纹、火花、蒸汽、研磨反光、声音等都要让玩家理解发生了什么；失败必须可读。
- **评判执行，不评判意图**：操作精度有客观后果，例如低温重锤更易积累损伤、淬火时机错误会影响热处理、研磨不均会留下刃口问题。玩家想锻成匕首、弯刀或奇怪的“像斧子的剑”都合法，系统不以预设造型评判其价值。
- **下游用后果说话**：玩家过程界面不显示“优秀/失败”总评；未来宿主通过可追溯的属性或事件反馈锻造后果。
- **每个工序形成决策闭环**：检查点必须说明玩家当下的目标、可用动作、阻力与取舍、可见信息、实际状态变化，以及它会怎样影响后续工序。功能能运行但不能改变玩家判断，不算玩法验证完成。
- **手感分层投入**：操作确认和状态可读属于当前规则的必要接口；风险提示在 R1 整链中验证；只增强爽感的震屏、音效、镜头与正式美术在核心闭环成立后集中打磨。

R1 八类能力：

```text
选料 / 切割 / 焊合 / 加热 / 锤击 / 淬火 / 回火 / 研磨
     -> ForgeState -> ForgeFacts -> 后续宿主规则
```

正火、受控装配、动力锤、图纸、复杂折叠焊和自由焊接都在 R1 稳定后讨论；它们不得提前挤占核心验证时间。

## 4. 游戏物理、形变与下游事实

### 4.0 真实物理与游戏物理的平衡点（Demo 执行准则）

**平衡原则：真实在因果结构，游戏在精度与可读性。**

- 保留真实锻造的「因果骨架」：敲哪里 → 哪里形变；何时淬火 → 硬度与脆性；怎么磨 → 刃口。这些决定玩家选择是否有意义，不得退化成「按按钮 +1 锋利」。
- 数值与阈值是游戏旋钮，不是物理常数：保留定律的「形状」（辐射 T⁴、Arrhenius 氧化、温变比热）以产生正确的曲线形态（边际递减、过热失控），但系数、阈值、时间倍率按「玩家能在一局内看见并学会差异」调。
- 每条物理后果必须有可读信号：不出现玩家看不见的裂纹、无预兆的失败。

各工序落点：

| 工序 | 保留的真实因果 | 游戏化的部分 | Demo 决定 |
| --- | --- | --- | --- |
| 加热 | 对流+辐射热学形式、温变比热 | 时间倍率压缩、温度是游戏可读的热色 | 整块均温（标量温度）；局部温差后置为第一精修项，接口预留 |
| 锻打 | 空间因果、体积守恒、热塑冷裂 | 应力/损伤/裂纹阈值是游戏旋钮 | 保留现有 XPBD 求解器，不加深 |
| 淬火 | 冷却介质与起始温度影响后续材料结果 | R1 保存有序事件 | 暂不求解相变和最终硬度 |
| 研磨 | 去除材料、形成连续表面 | 覆盖率、均匀度和去除体积是降阶量 | 暂不做表面重网格 |
| 下游 | 从真实状态事实推导，每项可追溯来源 | 属性维度由宿主定义 | R1 后先做窄探针 |

**可调性承诺**：所有平衡相关数字集中在 `src/data` 与 `FORGE_RULES`；作者在体验时给的建议 = 改数据表，不是重写代码。

### 4.1 采用的“够用且可扩展”的模拟

不做有限元、软体物理或真实化学。我们保存对玩家操作和后续消费真正有用的原始状态；权威字段分类见 `docs/FORGE_SYSTEM_SPEC.md`。

| 状态 | 保存什么 | 玩家能看见什么 |
| --- | --- | --- |
| 材料 | 密度、可硬化潜力、热参数、基础韧性等 | 材料外观、加热与成形手感差异 |
| 几何 | 截面宽度、厚度、长度分布、中心偏移、质量分布 | 刀尖、刀腹、刀背、刃口、弯曲、柄茎与重心 |
| 热处理 | 温度、塑性、有序淬火与回火历史 | 热色、冷却与可读工艺记录 |
| 完整性与表面 | 应力、损伤、裂纹、研磨连续性、表面完成度 | 裂纹、卷刃风险、磨痕、金属光泽 |

钢坯使用真实尺寸的三维规则网格，当前动力锤后薄坯为 `336 × 48 × 8`，单元边长为 2 个世界单位，共 `168 × 24 × 4` 个六面体。相邻单元在长、宽、厚三个方向共享节点；每块记录局部体积、温度、塑性、应力和损伤，几何尺寸与中心位置从共享节点推导。渲染读取同一网格的边界节点形成封闭实体。首版支持刀尖、刀腹、刀背、刃口、弯曲和简单柄茎；不支持开孔、分叉和任意拓扑。

每次锤击只求解锤面覆盖区与外围缓冲单元：带圆滑边缘的冠形锤面先接触当前高点，再随锤面下降扩大接触集合；有限砧面只为实际落在其投影内的材料提供反向支撑；单元有向体积约束令受压材料向自由方向流动；接触摩擦限制贴近锤面和砧面的切向滑移。热且可塑时锤面能推进得更深，低温重击更容易积累应力与损伤。实际几何体积必须在数值容差内守恒，所有操作固定迭代并可确定性回放。

整块均温的首版加热采用热量守恒：`m * Cp(T) * dT/dt = Q_convection + Q_radiation`。质量来自材料密度与实际体积，表面积来自当前共享节点外表面；炉内读取炉气和炉壁温度，炉外读取环境温度。辐射项使用开尔文四次方，`Cp(T)` 使用材料的分段 Shomate 系数。物理时间固定步长积分，游戏只用独立倍率压缩等待时间。氧化剂量使用温度相关的 Arrhenius 时间积分，当前只记录事实，不渲染氧化皮。

这不是“认真热锻/乱敲”这样的主观按钮扣分。测试中可以用“正确操作序列、低温重击、单侧重击”等描述场景；游戏里结果只来自玩家实际输入和当前材料状态。

### 4.2 参数化伪形变与画面反馈

玩家需要相信“我砸这里，这里真的变了”，但不需要真实材料仿真。技术上用截面参数驱动 Three.js 动态网格：物理截面是事实，视觉截面只是平滑插值。

每次落锤必须在 100ms 内出现局部形变、热色变化、火花与锤击声；淬火出现蒸汽；裂纹有清晰的视觉与声音提示。连续 30 次锤击不得出现断层、空白或报错。若动态网格在两天内无法稳定，画面可降级为连接式 2D ribbon，但不得改变 `ForgeState` 与 `ForgeSnapshot` 接口。

研磨沿刃口滑动，先以程序化表面状态表现“暗哑覆盖被逐步磨开”：覆盖率影响刃口完成度，均匀度影响外观与局部缺陷。不是点击一下就直接加锋利。

### 4.3 下游 profile 是翻译层，不是根基属性

R1 归档后，用一个窄 profile 检查 `ForgeFacts` 是否足以推导可追溯的宿主属性。以下六维只保留为曾讨论的候选示例，不是已批准的 R2 交付：

| 六维 | 主要来源 | 故事中的可见后果 |
| --- | --- | --- |
| 锋利 | 刃口几何、研磨连续性、硬度 | 切割、砍断、卷刃 |
| 硬度 | 材料与淬火状态 | 抗变形、硬碰硬、破甲 |
| 韧性 | 材料、热状态、损伤与裂纹 | 格挡、久战、恶劣环境、断裂 |
| 重量 | 密度与实际体积 | 跋涉、敏捷、偷袭、桥梁负担 |
| 平衡 | 质量分布与重心 | 连续动作、疲劳、失衡 |
| 外观 | 几何协调、表面完成度、工艺痕迹 | 威慑、社交、身份误判 |

若未来采用这些维度，调试入口只供作者检查公式输入、结果和来源。宿主派生的 `traits` 与 `flaws` 应结构化保存；它们不能反向污染锻造状态。

物理取舍必须存在，但具体维度和权重由后续样例验证，不能先假定六维已经正确。平衡和外观之类的解释是宿主判断，不是锻造核心直接奖惩任意形状。

特殊或魔法功能（如引火、认主）不伪装成物理事实；它们应由未来扩展事实或宿主规则表达，并经过独立契约设计。

## 5. 候选下游：故事、成长与分享

本节是产品假设库，不是当前执行路线。只有窄下游探针通过且作者重新拍板后，才会拆成正式阶段和验收标准。

### 5.1 故事闭环假设

若后续选择故事作为消费方式，可先用一个 NPC、三类情境和少量确定性事件验证。故事只能读取宿主派生数据和随机种子，不得读取玩家点击过程或编造锻造事实中不存在的缺陷。

故事的乐趣来自反差、因果、代入和稀有结果。例如外观出众但锋利差的武器可以威慑强盗，却砍不断障碍；自己造成的裂纹会在冒险格挡时断裂。冒险“难”主要是结局稀有度，而不是让大部分武器无法走完流程。

若选择轻跑团方向，可采用“宿主派生数据 + 世界状态 + 玩家选择 -> 条件规则 -> 事件 -> 结局”；避免剧情指数级分叉。具体输入维度必须在后续重新设计。

### 5.2 成长与社交（后置）

后续材料与工具提升的是工艺上限和损耗控制，不应该硬锁基本操作。图纸是可忽略的新手提示，不是唯一合法配方。失败可以产出裂纹和滑稽故事，是内容而不是纯惩罚。

候选出口包括玩家观看 NPC 使用结果或分享给好友。是否采用及何时实现由后续产品决策决定；R1 不处理服务端、防篡改或分享。

## 6. 画面与美术方向

2.5D 是降低资产成本并保留真实感的方式，不是把游戏退回纯 2D。近景锻打强调金属形变、温度、火焰和工具；工坊总览强调器具可读性；冒险画面后期可用剪影分层强化武器焦点。

视觉借鉴《小丑牌》的优点：信息层次清楚、反馈鲜活、有独立气质、像手工打磨过；**不复制**其 2D 卡牌美术。目标是略带像素颗粒/后处理、清晰描边、金属高光、暖色炉火主光源与统一的锻造奇幻气质。避免纯低保真像素和纯扁平矢量。

首轮完全程序化生成，不使用外部模型或贴图。正式体验阶段再通过代码生成的描边、光照、色板、火花、蒸汽和程序音效建立统一风格。未来如引入正式美术，先做一个“风格样张”作为所有资产的参照；风格统一优先于单张精美。

美术介入节奏：R1 只保留能支持操作判断的基础金属与热色，具体构图和体验由作者验收；器具精细建模与统一风格后置。已确认的铁砧空间经验可以保留为参考，但不由 AI 在本轮继续主观调整。

## 7. 架构契约与数据归属

```text
BrowserRuntime / WechatRuntime -> GameApplication
平台输入 -> ForgeIntent -> ForgeOperation -> ForgeState
                                      |-> ForgeSnapshot -> render
                                      `-> ForgeFacts -> host-owned profile
```

公共契约：

- `ForgeIntent`：平台无关的送料、转面、落锤、蓄力、拖动淬火、研磨等意图。
- `ForgeOperation`：合法性判断后的确定性操作记录，可保存和回放。
- `ForgeState`：材料区域、几何、热与工艺历史、界面、完整性、表面、机械响应事实和双版本。
- `ForgeSnapshot`：仅供渲染读取的几何、热色、裂纹与表面数据。
- `ForgeFacts`：仅供宿主规则读取的锻造事实，不包含攻击力、六维、故事或魔法结论。
- 宿主输出：由未来真实使用场景自行定义，当前不锁定 `WeaponData` 或 `StoryResult` 结构。

核心材料与物理参数集中在 `src/forge` 的规则表；未来宿主属性、故事节点与文案由宿主拥有并做运行时校验。纯逻辑模块禁止导入 Three.js、浏览器对象或微信 API。渲染只能读取快照，不能反向改数值。

### 7.1 可迁移边界

可迁移的目标不是把整个游戏做成通用引擎，而是让已经验证的锻造核心能被另一个宿主调用：

- `forge` 及其规则不得依赖 `app`、`story`、`render`、浏览器、微信或《打了个铁》专用文案。
- 宿主通过明确的意图、操作、状态、快照和事实出口使用核心；公共契约可序列化、带版本，并有固定样本和确定性测试。
- 平台输入、Three.js 表现、故事内容和产品流程属于适配或应用层，不反向进入核心规则。
- 在第二个真实使用场景出现前，不新增插件系统、通用编辑器、动态扩展框架或独立 npm 包；需要复用时先写出具体接入需求，再决定最小抽取范围。
- R1 至正式体验阶段的内部重构只服务当前可玩切片；正式独立打包、授权或合作交付属于产品化阶段。

## 8. 阶段计划与作者验收

凡检查点涉及新玩家行为或体验验收，AI 的交付回复必须先给作者一份“阶段对齐报告”，不能只汇报写了哪些文件：

```text
当前阶段与检查点：在总计划中的位置
原计划目标：本切片原本要证明什么
本次实际完成：玩家现在确实能做什么
已有证据：自动检查、可观察结果与作者已确认项
尚未验证：不能宣称完成的内容
偏离判断：无偏离，或范围 / 架构 / 阶段 / 证据 / 精度偏离
当前体验精度：操作确认 / 状态可读 / 风险可读 / 表现打磨中本阶段应达到哪层
后置但保留：本阶段不精修、以后仍必须处理的手感与内容
推进判断：进入验收 / 修改当前切片 / 停止并解决偏离
```

阶段对齐报告之后，AI 再给出与当前切片对应的作者测试清单，固定写明：准备入口、操作步骤、应当看到、不应看到、需要作者判断的体验项、通过标准和完成后停点。技术测试不能代替作者对新玩家行为、手感、数值、美术或故事因果的验收。没有项目适配 Skill 时也必须执行这一格式；Skill 只能帮助生成，不能取代本文件的强制要求。

阶段推进只追求当前目标所需的完成度：R1 证明八类操作、原始状态和工序传递；随后用一个窄下游契约探针证明事实可被有意义地消费；通过后集中精修。故事、订单、成长和商业化阶段需要作者另行拍板，不能沿用旧编号自动开工。

### R0：双运行时技术门

目标：先证明 Three.js 方案在浏览器和微信小游戏都能使用同一份状态与渲染逻辑，不把玩法建立在未知兼容性上。

交付：固定斜俯视相机、程序化灯光、铁砧、24 截面钢坯；浏览器点击或微信触摸钢坯后局部截面确定性变化。

验收：浏览器自动测试通过；微信开发者工具可编译显示；作者真机预览可触摸并看到变化；两端回放同一操作得到相同 JSON；微信包体合规；Windows 全新 clone 能重新构建两个产物。

### R1：最小可玩锻造链（当前 Demo 已扩展为八动词切片）

目标：在统一状态底座上跑通选料、加热、锻打、淬火、回火、研磨、切割和焊合；工坊总览、故事和正式美术仍后置。

交付：两套输入产生同一种意图；局部锤击影响相邻截面；温度、形变、应力、损伤、淬火、回火、切割、焊合和研磨状态可见；同一回放跨平台一致。浏览器 MVP 已将八个工序接入总览与独立工位近景；微信端等价八工位玩法仍未完成。

验收：作者可连续锻打 30 次不出错；高温与低温、对称与单侧锻打产生肉眼可见且数据可解释的不同；完整最小链能导出一份可评估的最终状态。整链验收同时记录各工序的等待、重复操作、思考时间与有效决策，不先拍脑袋规定整局时长。

### R2：窄下游契约探针

目标：选择一个很窄的真实宿主问题，把 R1 的 `ForgeFacts` 稳定翻译成少量有意义、可追溯的结果，检查事实是否充足。

交付：一个宿主 profile、明确版本、少量对照样本和来源说明；只读取 `ForgeFacts`，不修改锻造状态，不提前制作完整属性体系或故事内容。

验收：同一状态得到相同结果；改变相关锻造事实会按可解释方向改变宿主结果；若现有事实不足，先报告缺口，不用 profile 猜测或回写核心。

### 后续候选：完整下游玩法

故事、订单、动作战斗属性、魔法扩展、成长和分享均是候选方向。R2 探针通过后，由作者选择其中一个重新定义目标与验收，不默认故事优先。

### R3：正式体验与作品集

目标：把已验证的链条做成完整可展示体验。

交付：2.5D 工坊总览、器具入口、反馈打磨、程序化视觉风格、存档与分享数据、试玩记录、演示录像、规则样本与系统设计复盘。完整链成立后再根据实测工序时长、有效决策和玩家困惑调整节奏，不把单纯等待误当作游戏时长。

验收标准在 R2 后根据选定宿主玩法制定。至少要能观察玩家是否理解操作因果、是否会在第二次锻造中主动调整策略；小样本只用于发现问题，不包装成市场统计结论。

### R4：锻造核心产品化与权利准备

目标：在《打了个铁》核心闭环和第二个真实使用场景验证后，判断锻造系统能否作为独立资产复用、授权或合作开发；本阶段不反向扩大 R1。

交付：一个脱离《打了个铁》故事与界面的最小接入样例；经过验证的核心模块边界、公共契约、版本规则、依赖与来源清单、集成说明；针对第二个真实使用场景的差异分析；源码授权、闭源交付、定制开发或联合开发的方案比较。

验收：第二个宿主能在不导入《打了个铁》应用与故事代码的情况下重放操作、读取状态并获得评估结果；核心修改不会破坏原游戏固定样本；作者明确选择是否以及如何对外提供，未获授权前不发布独立包、不承诺商业许可。

## 9. 产品化与产权治理

产权治理的目的，是保存独立创作证据、厘清来源与合作边界，并降低未来授权争议；它不能保证垄断抽象玩法，也不能由 AI 代替专业法律判断。

### 9.1 从现在持续保留的证据

- Git 提交、PR、版本标签、计划变化、规则样本、自动测试、作者验收、试玩录像和演示产物共同形成连续创作记录；不另建重复的流水账文档。
- 代码、规则数据、设计文档、文案、美术、音频和生成内容都要能说明来源、作者、许可与修改情况；权属不清的内容不进入可交付版本。
- 第三方依赖必须锁定版本并记录许可证；未来引入模型、贴图、字体、音效或外部代码时，先确认是否允许商业使用和再分发。
- 公司代码、内部资产、保密文档和权属不清的职务成果不得进入本项目。鉴于项目曾在公司设备上开发，商业合作前由作者核对劳动或实习协议，必要时咨询知识产权专业人士。
- AI 可以辅助实现和整理，但产品目标、关键取舍、作者验收与版本归档必须保留人的决定和可验证证据。

### 9.2 对外动作的三道检查门

| 时机 | 必须确认 | 未确认时的边界 |
| --- | --- | --- |
| 当前开发 | 仓库公开范围、既有公开内容、公司协议与第三方来源风险 | 不因本计划自动修改仓库可见性或删除历史 |
| 对外合作前 | 保密范围、参与者职责、既有成果、共同改进成果、使用地域/期限、再许可和退出方式 | 不只依赖口头约定，不向合作方交付完整核心源码 |
| 稳定发布或授权前 | 软件著作权登记、商标、许可证、商业合同，以及是否存在值得专业评估的专利或不正当竞争保护路径 | AI 只整理材料与风险，不给出法律有效性保证 |

当前 GitHub 仓库为公开仓库且没有 `LICENSE` 文件，这是已知的披露与合作风险，不等于他人当然取得使用许可，也不能阻止他人看到实现。是否转为私有、采用何种许可证或调整公开内容，必须另设检查点，由作者在了解既有公开无法撤回的前提下决定。

## 10. 微信适配止损线

微信与浏览器有不同运行入口，共享 `GameApplication` 和 `forge` 核心；浏览器当前使用八工位 `ForgeBilletView`，微信当前使用基础 `FurnaceView` 火炉验证入口。未来两端可以继续共享核心状态与规则，但当前不能宣称玩法入口等价。渲染器显式接收画布与 WebGL 上下文，游戏自行驱动帧循环。

如果同一适配路径出现两次无法解释的失败，或适配修复超过一个完整工作日，必须停止玩法开发并给作者风险报告，不能悄悄扩展成通用微信引擎。

微信开发者工具登录、导入、真机扫码和真机验收由作者完成；AI 的交付是可导入工程、明确操作说明和浏览器侧证据。

## 11. 开发环境与交接

- 当前唯一开发设备为私人 Windows；旧公司 macOS 已完成交接，不再拥有未推送工作。
- Windows 使用 Node `v24.19.0`、npm `11.17.0`。`main` 与火炉 WIP 均已分别通过类型检查、逻辑测试、浏览器/微信构建和对应 Playwright 端到端测试。
- GitHub CLI 已登录；微信开发者工具的安装、登录、导入与真机预览仍由作者操作，完成前不得声称微信真机验收通过。
- 每次离机前运行可用检查、更新本文件、推送已验收提交或明确的 `[WIP]`，并确认本地与远端提交一致。接手设备先读 `AGENTS.md`、本文件和 Git 历史，再执行 `npm ci` 与 `npm run check`。

## 12. 现状、废弃项与下一步

本次重启保留 Git 历史，但当前执行只以现行文件树、`AGENTS.md` 和本文件为准。旧 Cocos 路线、旧文档结构、旧模块编号和旧半成品均已退出现行文件树；它们只作为历史提交保留，不得恢复到新功能中。

已确认的变更只有三项：

1. 引擎从 Cocos Creator 改为 TypeScript + Three.js。
2. 浏览器不再是之后会被丢弃的原型，而是与微信共享代码的持续开发入口。
3. 当前执行顺序是八项 R1 验收、整链回归、窄下游契约探针、集中精修；具体下游玩法另行决策。

`R0.5` 至 `R1h-3a` 的已验收 Three.js 工作通过 PR #9 归档到 `main`。当前 `feat/R1i-furnace-heating` 包含八工位浏览器 MVP、基础数据纠偏和作者验收清单；仍等待作者逐项体验，不能视为 `main` 稳定版本。具体提交关系与检查结果必须以当前命令核对，微信真机预览仍需作者操作。

## 13. 模型与设备交接摘要

下一位 AI 必须先阅读 `AGENTS.md` 与本文件，并执行 `git status --short --branch`；不得把聊天记录当作项目事实。开始修改前确认本地与远端一致，不直接改 `main`，不重置或清理历史。

当前可试玩入口由 `npm run dev -- --host 127.0.0.1 --port 4177` 启动。已知检查命令是 `npm run typecheck`、`npm run test`、`npm run test:e2e`、`npm run check`。项目依赖安装使用 `npm ci`；运行环境为 Node.js 24。微信开发者工具的登录、导入与真机预览只能由作者操作，AI 不得声称已替作者完成。

自动验证范围包括：浏览器与微信构建、八工位入口和玩家输入路径、焊合对象选择与来源保留、质量变化、热处理事件、确定性状态、序列化与事实出口。每次报告必须给出本次实际运行结果，不能沿用本段作为通过证据。当前等待作者判断：八项操作体验、状态与风险是否可读、物理真实度和第一人称表现。微信等价八工位、正式下游、正式美术与音效仍未实现。

严格保留的产品共识：这是 2.5D、程序化 Three.js 的微信小游戏项目，浏览器是同源调试和展示入口；不使用 Cocos、Unity、外部场景编辑器、GLB 或贴图。锻造不是主观扣分，宿主结果必须从材料、几何、热历史、界面、完整性和表面等可解释事实推导。具体下游维度未锁定；首版不做有限元、软体物理、真实化学、任意拓扑或运行时 LLM。
